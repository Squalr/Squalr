// Slint works by compiling things that inherit from Window, so these types are consoledated here such that Rust can compile them
export { InstallerWindowView } from "installer_window/installer_window_view.slint";

// Export all view model bindings such that Rust can interact with each view type through globals.
export * from "view_model_bindings.slint";
