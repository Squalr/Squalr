# Agentic Current Task
Our current task, from `README.md`, is:
`pr/todo`

# Notes from Owner
- Assume any unstaged/uncommitted file changes are from a previous iteration, or from the human author giving guidance. Keep them if they look good; do not ask about them by default.
- Assume any connected Android devices are rooted, and assume macOS has SIP disabled.
- Do not declare behavior as fixed. Use "needs human verification" after implementation and validation.
- Alpha-stage data compatibility is not required for this refactor. Prefer a clean model over preserving old address/pointer/symbol-ref project item properties.

## Current Tasklist

- Completed: Project item runtime value display formats are now persisted separately from the preview value, and Project Explorer refresh/detail focus preserves the chosen format.
- Completed: Output dock command input now draws a 1px border and uses a compact 28px height with 4px spacing above it.
- Completed: Relative element scan constraints now omit hidden value-box contents, preventing empty literal parsing after Collect Values when scanning increased/decreased/changed/unchanged.
- Completed: Removed the Symbol Tree per-node manifest display-format persistence path.
- Completed: Symbol layout fields can now store an optional preferred display format, exposed only when the field data type reports supported display formats.
- Completed: Symbol Tree runtime/preview values consume the layout-owned preferred display format but do not allow display-format edits from the Symbol Tree details view.
- Completed: Symbol Tree module-root layout presentation now consumes promoted symbol claims that correspond to module-root layout fields without appending duplicate claim nodes or inserting extra undefined segments for claim-relocated sequential layout fields. A winmine-shaped regression covers the exact `PE Headers`, undefined, `winmine_exe_0x579C`, undefined order.
- Completed: Prompt command error/help usage now strips executable/crate names for GUI and interactive CLI session mode, showing top-level prompt usage as `<COMMAND>` while preserving executable-style usage for CLI one-shot errors.
- Completed: Valueless relative scan constraints now compile into real relative constraints instead of being dropped, so Collect Values followed by Increased/Decreased filters against the collected baseline rather than retaining every result.
- Completed: Linux windowed-process detection now recognizes display-connected client sockets as a best-effort Wayland/X11 signal, so the Linux GUI process appears in `--require-windowed` process queries under WSLg.
- Completed: GUI logging now suppresses noisy dependency debug/span targets and the repeated WSLg Wayland cursor warning target while preserving Squalr root debug logging.
- Completed: Linux module enumeration now spans all mappings for a file-backed executable module instead of only the executable segment, allowing static addresses in ELF data/rodata mappings to resolve as module offsets for promote-to-symbol.
- Completed: Project item path resolution now accepts project-rooted relative paths without prepending the opened project directory again, and rebases normalized project-root aliases back onto the opened project directory's exact path form, preventing promote-to-symbol from looking for duplicated paths or missing `./Squalr/...` map keys as `Squalr/...`.
- Completed: Binary header symbol population now supports richer ELF module roots through the generic Populate Binary Symbols action, naming program/section/dynamic entries and resolving interpreter, dynamic string, and dynamic symbol metadata when readable.
- Completed: Symbol Layout Editor field removal is now presented as Unassign for struct fields, preserves the removed field's byte span as an in-place unassigned block without shifting following sequential fields, allows unassigning the only struct field, and keeps true layout deletion labeled Delete.
- Completed: Android privileged IPC now keeps the worker alive without relying on stdin, uses a Serde-compatible JSON payload codec instead of bincode for command/event payloads, and the deploy script clears stale worker processes before launching a fresh APK.
- Completed: Android process selector now defaults the full process selector to full-list mode, keeps the quick-select dropdown strictly windowed-only, and sends an Android soft-keyboard viewport command while egui has text focus so value boxes can request the IME.
- Completed: Android process switching now clears the displayed process immediately and repaints into the existing `Opening process...` state while the open request is pending; Android project creation now uses an app-private writable project catalog and stops the create flow on failed responses.
- Completed: Android data value boxes now request the platform soft keyboard directly from the Android app handle on editable focus/click and retry briefly while egui reports keyboard focus.
- Completed: Android logging now initializes through the shared `LogDispatcher` instead of preempting it with `android_logger`, so the GUI Output dock can read the same in-process log history while Android still mirrors logs to logcat. The Output dock command input now explicitly requests the Android soft keyboard on focus/click.
- Completed: Android GUI packaging now uses the `eframe`/`android-activity` GameActivity backend and a generated Gradle APK under `target/android-gameactivity-gradle`, avoiding the NativeActivity text-input path that ignored GameActivity's richer input state APIs.
- Completed: Android text input now uses a workspace patch for `winit 0.30.13` under `third_party/winit-0.30.13`, keeping upstream Android key-event text population and adding a narrow GameActivity `TextEvent` delta bridge. The bridge diffs the hidden GameActivity text state, normalizes the observed duplicate soft-keyboard character pairs, emits inserted text as synthetic winit key-text events for egui's normal `Event::Text` path, and emits deletions as synthetic Backspace press/release events.
- Completed: Android GameActivity IME enter/newline deltas now emit synthetic Enter press/release events, single-line data value boxes commit and surrender focus on focused Enter, and `android_activity::game_activity` debug logs are capped at info to suppress repeated input-available noise.
- Completed: Android privileged-worker logs now forward to the unprivileged host through structured logging engine events, then the host re-logs them with a `[privileged]` prefix so the GUI Output dock can display them. The repeated Android `Notifying Input Available` record is suppressed at the appender layer, and the `android_activity` logger family is capped at warn.
- Completed: Android text insertion now relies on GameActivity `TextEvent` deltas instead of also letting Android key events carry text, preventing soft-keyboard character duplication. Privileged log forwarding now ignores debug/trace records and uses a bounded non-blocking queue drained by a background thread, preventing scan progress debug logs from blocking element scans through synchronous IPC/log forwarding.
- Completed: Android privileged IPC response callbacks now run after the request-handle map lock is released, preventing the `ScanNewRequest` response callback from deadlocking when it immediately dispatches the follow-up `ElementScanRequest`. Element scan UI dispatch failures now warn and roll back the in-progress spinner state.

## Important Information

- Validation: `cargo fmt --all` completed with existing rustfmt deprecation warnings for `fn_args_layout`; `cargo test -p squalr build_scan_constraints` passed 2 targeted tests; `cargo test -p squalr` passed 31 tests. Prompt validation: `cargo test -p squalr-engine-api` passed 303 tests; `cargo test -p squalr-cli` passed 14 tests; `cargo test -p squalr output_command` passed 4 targeted tests. Scan validation: `cargo test -p squalr-engine` passed 154 tests. Binary symbol validation: `cargo test -p squalr-plugin-binary-symbols -- --nocapture` passed 26 tests; `cargo test -p squalr-engine execute_plugin_action -- --nocapture` passed 3 targeted tests; WSL `cargo test -p squalr-plugin-binary-symbols -- --nocapture` passed 26 tests. Linux validation: WSL `cargo test -p squalr-engine-targets-native linux_process_query -- --nocapture` passed 14 tests; WSL `cargo test -p squalr-engine-targets-native linux_memory_queryer -- --nocapture` passed 8 tests; WSL `cargo test -p squalr-engine-targets-native -- --nocapture` passed 23 tests plus doctests; WSL `cargo test -p squalr-engine promote_symbol_request -- --nocapture` passed 10 tests; WSL `cargo test -p squalr-engine project_item_file_mutation -- --nocapture` passed 9 tests; Windows `cargo test -p squalr-engine promote_symbol_request -- --nocapture` passed 10 tests; Windows `cargo test -p squalr-engine project_item_file_mutation -- --nocapture` passed 9 tests; `cargo test -p squalr-engine-session logging -- --nocapture` passed 1 test; WSL `cargo build -p squalr -p squalr-cli` and WSL `cargo build -p squalr` passed; runtime WSL check launched `squalr`, verified `squalr-cli process list --require-windowed --search-name squalr` returned `is_windowed: true`, and verified zero `winit::Window::`, `Failed to set cursor`, or `tracing::span` matches in the captured GUI log. Symbol layout unassign validation: `cargo fmt` completed with the existing `fn_args_layout` deprecation warnings; `cargo test -p squalr-engine-api symbol_layout` passed 15 targeted tests; `cargo check -p squalr` passed. Android IPC/process-list validation: `cargo fmt --all` completed with existing `fn_args_layout` warnings; `cargo test -p squalr-engine ipc_payload_codec_round_trips_registry_catalog_with_symbolic_layout` passed; `cargo test -p squalr-cli` passed 14 tests; `python -m py_compile .\scripts\build_and_deploy.py` passed; `python .\scripts\build_and_deploy.py --debug --launch-log-seconds 8 --launch-log-file .\target\android-launch-logcat-final.txt` completed deployment and smoke validation on attached device `4C101FDKD000Z8`; logcat showed windowed process listing returned `com.squalr.android` and full process listing returned process rows, with no filtered `PayloadDeserializationFailed`, `Broken pipe`, or windowed process-list dispatch failure after the final deploy. Android process selector/IME validation: `cargo fmt --all` completed with existing `fn_args_layout` warnings; `cargo test -p squalr process_selector_view_data -- --nocapture` passed 2 targeted tests; `python .\scripts\build_and_deploy.py --debug --launch-log-seconds 8 --launch-log-file .\target\android-launch-logcat-ime-selector.txt` deployed and smoke-validated on attached device `4C101FDKD000Z8`; logcat checks for `/data/local/tmp/squalr-cli process list --require-windowed --search-name com.squalr.android` and without `--require-windowed` both returned `process_id: 16844, name: com.squalr.android, is_windowed: true`. Note: the CLI response handler logs to Android logcat rather than stdout, so `verify_android_windowed_processes.py` can report false negatives unless adapted to read logcat.
- Latest validation: `cargo fmt --all` and `cargo fmt --manifest-path third_party/winit-0.30.13/Cargo.toml` completed with existing `fn_args_layout` warnings; `cargo test -p squalr-engine-session remote_forwarding_skips_debug_progress_logs -- --nocapture` passed 1 targeted test; `cargo ndk --target aarch64-linux-android test --manifest-path third_party/winit-0.30.13/Cargo.toml --no-run --no-default-features --features android-game-activity,rwh_06 normalize_game_activity_text_delta` compiled the Android winit test harness; `cargo test -p squalr-engine ipc_payload_codec_round_trips_privileged_log_event -- --nocapture` passed 1 targeted test; `cargo check -p squalr` passed; `cargo check -p squalr-engine` passed; `python -m py_compile .\scripts\build_and_deploy.py` passed. `python .\scripts\build_and_deploy.py --release --launch-log-seconds 8 --launch-log-file .\target\android-launch-logcat-input-scan-fixes-release.txt` built and deployed the release Android CLI/APK path to attached device `4C101FDKD000Z8`, launched the app, detected privileged worker PID `25752`, and completed smoke validation. The captured filtered logcat showed `[privileged]` info logs and did not contain `Notifying Input Available`, `Element scan progress`, or `Value collection progress`. Runtime Android text insertion and element-scan UI responsiveness still need human device verification.
- Latest Android scan-deadlock validation: `cargo fmt --all` completed with existing `fn_args_layout` warnings; `cargo test -p squalr-engine response_callback_runs_after_request_handle_lock_is_released -- --nocapture` passed 1 targeted test; `cargo check -p squalr-engine` passed; `cargo check -p squalr` passed; `python -m py_compile .\scripts\build_and_deploy.py` passed; `git diff --check` passed with line-ending warnings only. `python .\scripts\build_and_deploy.py --release --launch-log-seconds 8 --launch-log-file .\target\android-launch-logcat-ipc-deadlock-fix-release.txt` built and deployed the release Android CLI/APK path to attached device `4C101FDKD000Z8`, launched the app, detected privileged worker PID `26197`, and completed smoke validation. Startup logcat still showed forwarded `[privileged]` records. Runtime Android element-scan completion still needs human device verification.
- Human verification: Reopen the winmine project and confirm the Symbol Tree shows `PE Headers`, one undefined segment, `winmine_exe_0x579C`, and one trailing undefined segment under `winmine.exe`, matching the single layout field shown by right-click edit layout. Also confirm the previous display-format and Output dock checks still behave as expected. In the GUI Output dock, entering `24` should report `Usage: <COMMAND>` rather than `Usage: squalr-engine-api <SUBCOMMAND>`. In the scanner, Collect Values followed by Increased should only retain entries whose scan-time current value is greater than scan-time previous value. On Linux/WSL, confirm the GUI's windowed process filter includes the running Squalr process and the Output log is not flooded with winit span/cursor messages during normal idle interaction. Recreate a Linux static project item from a fresh scan result in the rebuilt GUI, promote it to a symbol, and confirm the Symbol Tree creates the module root layout/field without logging a missing duplicated project item path. On Linux/WSL, run Populate Binary Symbols on an ELF module root and confirm `ELF Headers` expands into named `PT_*`, section, `DT_*`, interpreter, dependency/soname/runpath, and dynamic symbol fields when those regions are readable from process memory. In the Symbol Layout Editor, unassign a middle struct field and confirm following rows do not shift; also unassign the only field in a fixed-size struct and confirm the visible row becomes an unassigned block at the same byte span. On Android, open the rebuilt GUI and confirm the quick-select windowed process list visibly contains only windowed processes, the full process selector starts with the Windowed toggle off and lists full results, the toggle switches to windowed-only results, and focusing editable data value boxes opens the soft keyboard. This needs human verification.
- Latest human verification needed: On Android, confirm soft-keyboard text enters exactly once per character, can enter more than one consecutive character, backspace works repeatedly, Enter/done commits single-line data value boxes and closes/surrenders focus, and text is inserted into the focused editable data value box and the Output command input in the GameActivity build. Also confirm switching processes immediately shows `Opening process...` instead of the previous process name, and confirm the Add Project button creates/selects a new project. Confirm the Output dock shows startup logs/command echoes, displays privileged worker records with the `[privileged]` prefix, and does not flood with `Notifying Input Available` or scan progress logs. Run an Android element scan and confirm the GUI remains responsive, the result spinner clears, and the privileged log shows value collection/element-scan phases after `Started new scan.` This needs human verification.
