# Agentic Current Task (Readonly)
Our current task, from `README.md`, is:
`pr/TODO`

# Notes from Owner (Readonly Section)
- Assume any unstaged/uncomitted file changes are from a previous iteration (or if this file, probably the human author giving guidance), and can be kept if they look good. Do not ask me about them.
- Assume any connected android devices are rooted, and assume MacOS has SIP disabled.
- You don't get to declare things as fixed. Only "need human verification".

## Current Tasklist (ordered)
(Remove as completed, add remaining concrete tasks. If no tasks, audit the GUI project against the TUI and look for gaps in functionality. Note that many of the mouse or drag heavy functionality are not really the primary UX, so some UX judgement calls are required).

- need human verification: Symbol Tree filler rows no longer use the `UnknownBytes` model or `unknown_...` display names. Module unclaimed spans are now `U8Segment` rows such as `u8_00000000: u8[length]`, are selectable derived tree rows, and can be promoted without forcing Details Viewer or preview reads over potentially huge byte ranges. Clicking a module root expander now also selects that module row, so F2 applies to the module itself after the normal project-tree-style click target. Promotion module sizing now prefers the queried virtual page containing the loaded module base, falling back to the module record size only if no containing page is available; the promote test asserts a module with a `0x2000` module record but a `0x5000` queried virtual page persists the `0x5000` module size. The symbol docs were cleaned to use `u8[]` / unclaimed-space wording rather than `unknown` terminology. Reverified with `cargo fmt --all`, `cargo check -p squalr-engine-api -p squalr-engine-projects -p squalr-engine -p squalr-cli -p squalr-tui -p squalr`, `cargo test -p squalr-engine project_items_promote_symbol -- --nocapture`, `cargo test -p squalr-engine project_symbols -- --nocapture`, `cargo test -p squalr symbol_explorer -- --nocapture`, `cargo test -p squalr symbol_table -- --nocapture`, and `git diff --check`.
- need human verification: Symbol Tree module roots are now selectable and inline-renamable with the standard F2/double-click flow. The new `project-symbols rename-module` command renames the persisted module root, rejects duplicate module names, retargets module-relative symbol claims by module name, saves the project, and resyncs the privileged symbol catalog. Creating a module selects and expands that new root, and the create form labels the numeric value as `Initial u8[] Size` to match the struct-space model. Symbol Tree expansion now treats module roots as collapsed-by-default roots, so a huge module remains a single row until expanded; expanded fixed arrays render only the first 256 children plus a truncation sentinel, and truncation/module rows no longer drive memory preview/read focus. Static promotion now creates/updates the module root and uses the loaded module size when attached, falling back to a conservative local type-size estimate for the claim extent so the initial unknown `u8[]` space can split around the typed symbol. Reverified with `cargo fmt --all`, `cargo check -p squalr-engine-api -p squalr-engine-projects -p squalr-engine -p squalr-cli -p squalr-tui -p squalr`, `cargo test -p squalr-engine project_symbols -- --nocapture`, `cargo test -p squalr-engine project_items_promote_symbol -- --nocapture`, `cargo test -p squalr symbol_explorer -- --nocapture`, `cargo test -p squalr symbol_table -- --nocapture`, `cargo test -p squalr-engine-api project_item_type_symbol_ref -- --nocapture`, and `git diff --check`.
- need human verification: Symbol Tree module roots are now real persisted `ProjectSymbolModule { module_name, size }` records, and the Symbol Tree `+` flow creates/upserts only those two fields. Empty modules render as expandable module roots with derived `u8[]` unknown space up to the stored size, and module-relative promoted claims render inside that module with `u8[]` gaps before/between/after typed claims. The Symbol Table no longer has a create-symbol form or plus action, and visible key/id rows were removed from Symbol Tree/Table detail surfaces. `ProjectSymbolClaim` no longer persists a separate symbol id/key; internal symbol-ref lookup uses a derived `symbol_locator_key` from the claim locator (`module:<module_name>:<offset>` or `absolute:<address>`), and project symbol commands/project-item symbol refs were renamed around that locator-key concept. Reverified with `cargo fmt --all`, `cargo test -p squalr-engine-api project_item_type_symbol_ref -- --nocapture`, `cargo test -p squalr-engine project_symbols -- --nocapture`, `cargo test -p squalr-engine project_items_promote_symbol -- --nocapture`, `cargo test -p squalr-engine project_items_convert_symbol_ref -- --nocapture`, `cargo test -p squalr-engine project_items_activate -- --nocapture`, `cargo test -p squalr-engine project_items_list -- --nocapture`, `cargo test -p squalr-engine-projects project_info_round_trip_preserves_symbol_claims -- --nocapture`, `cargo test -p squalr symbol_explorer -- --nocapture`, `cargo test -p squalr symbol_table -- --nocapture`, `cargo test -p squalr-tui project_explorer -- --nocapture`, and `cargo check -p squalr-engine-api -p squalr-engine-projects -p squalr-engine -p squalr-cli -p squalr-tui -p squalr`.
- need human verification: `docs/symbol_store.md` and `docs/symbols.md` now simplify the symbol UX around a literal Symbol Tree module struct model. The tree should start empty, module roots are added with `+`, module/field rename should use the standard F2 mechanism, and module root records contain only `module_name` plus `size`; there is no module id, stored address, path, hash, load state, or metadata for the basic Symbol Tree add flow. Module base addresses resolve live by module name. Typed fields and `u8[]` filler are expansion content under the module root, not extra root fields. Promotion from a static project item should create the missing module root, query and store module size when attached, seed `u8[module_size]`, and split that filler into typed fields. The docs explicitly reject attach-time module population and a static-candidate Symbol Table workflow for now. Reverified with `git diff --check`.
- need human verification: The reusable struct-layout authoring surface has been fully renamed to `SymbolStructEditor`: GUI dock/menu labels now say `SymbolStructEditor`, the dock id is `window_symbol_struct_editor`, the view module moved to `squalr/src/views/symbol_struct_editor/`, and the Rust view/data/helper types were renamed to `SymbolStructEditor*` / `SymbolStructField*` / `SymbolStructLayout*`. Reverified with `cargo fmt --all`, `cargo check -p squalr`, `cargo test -p squalr symbol_struct_editor -- --nocapture`, `cargo test -p squalr main_toolbar_view -- --nocapture`, and `cargo test -p squalr dockable_window_settings -- --nocapture`.
- need human verification: Symbol authoring has started moving from rooted-symbol vocabulary to symbol-claim/module-space vocabulary. `ProjectRootSymbol` / `ProjectRootSymbolLocator` were replaced by `ProjectSymbolClaim` / `ProjectSymbolLocator`, `ProjectSymbolCatalog` now stores `symbol_claims`, GUI/CLI/TUI user-facing rooted-symbol strings were removed, and the CLI now handles `project_symbols update` responses. The Symbol Tree builder now emits module-space rows, nests module-relative claims under their module, groups absolute claims under `Absolute / Unmapped`, sorts claims by offset, and synthesizes visible `unknown_XXXXXXXX: u8[length]` gaps before/between module claims based on derived claim size. Reverified with `cargo fmt --all`, `cargo test -p squalr symbol_explorer -- --nocapture`, `cargo test -p squalr-engine project_symbols -- --nocapture`, `cargo test -p squalr-engine-projects project_info_round_trip_preserves_symbol_claims -- --nocapture`, `cargo test -p squalr-cli -- --nocapture`, `cargo test -p squalr-tui project_explorer -- --nocapture`, and `cargo check -p squalr-engine-api -p squalr-engine-projects -p squalr-engine -p squalr-cli -p squalr-tui -p squalr`.
- need human verification: `docs/symbol_store.md` and `docs/symbols.md` now reframe the symbol model around modules as visible roots and symbols as typed claims over module memory. Unknown module space is represented as derived `u8[]` chunks, promotion is documented as a claim split/retype operation, exact static promotions replace unknown chunks in place, typed-overlap promotion requires explicit conflict handling, and `Rooted Symbol` is called out as compatibility/internal terminology rather than desired product language.
- need human verification: Symbol Tree no longer carries the redundant reusable symbol-type/struct-layout list now that SymbolStructEditor owns type authoring. Its selection model now only tracks rooted symbols, derived tree nodes, and rooted-symbol creation, so projects with struct layouts but no rooted symbols show the normal empty rooted-symbol state instead of selecting a type-only row. The `Windows` menu also has a divider before the Symbol Tree / Symbol Table / SymbolStructEditor group, while Details Viewer remains above that divider with the broader project/details panes instead of being grouped as a symbol authoring window. Reverified with `cargo fmt --all`, `cargo test -p squalr symbol_explorer -- --nocapture`, `cargo test -p squalr main_toolbar_view -- --nocapture`, and `cargo check -p squalr`.
- need human verification: Docked tab overflow now stays inside its pane instead of bleeding offscreen. The docking footer tab strip now clips to the footer bounds, reserves themed left/right arrow buttons only when the sibling tabs overflow the available width, keeps a small per-tab-group horizontal scroll offset so arrow nudges persist while you stay in that tab group, and auto-scrolls the selected active tab back into view. Tab labels are measured from their actual titles and clipped inside each tab button instead of painting freely across neighbors.
- need human verification: Docked tab overflow now stays inside its pane instead of bleeding offscreen. The docking footer tab strip now clips to the footer bounds, reserves themed left/right arrow buttons only when the sibling tabs overflow the available width, keeps a small per-tab-group horizontal scroll offset so arrow nudges persist while you stay in that tab group, and auto-scrolls the selected active tab back into view. Tab labels are measured from their actual titles and clipped inside each tab button instead of painting freely across neighbors. The footer now also resolves active-tab visibility against the current viewport before rendering the arrow buttons, so resize should stop leaving the left/right enabled state a frame behind the actual post-resize scroll offset.
- need human verification: Docked tabs now support runtime call-to-action attention states. The docking layer has a generic per-window attention map (`warning` / `danger`) that the footer renders as a slow pulse toward the theme warning/danger colors, clicking a tab clears its attention state, and non-forced attention auto-clears if the tab is already the active visible tab. `MainWindowView` now polls new log events each frame and raises Output tab attention on fresh `warn` / `error` entries (upgrading to danger for errors, but skipping the animation if Output is already the active tab), and it also continuously reasserts a forced warning attention state on the docked `Project Explorer` tab while no project is open, clearing that CTA as soon as a project is opened.
- need human verification: SymbolStructEditor create/edit/delete now use a real full-panel takeover instead of an overlay popup. When a takeover is active, the normal list surface no longer renders underneath it at all; the takeover owns the whole dock pane content area and uses a simple full-width header plus scrollable body instead of a centered floating card/window. The takeover styling now brings back `GroupBox` only at the top section level, following the Settings page rhythm: `Struct Layout` / `Fields` / `Confirmation` are boxed, but the inner field editors themselves remain plain stacked sections with separators instead of nested groupboxes. The field header row also no longer inserts a literal `available_width()` spacer before the delete action, which was pushing the remove button and other field-row content past the right edge, the last remaining draft field now keeps the delete button visible in its standard disabled state instead of hiding it, new draft fields now default to `i32` instead of `u8`, the add/edit field rows are more compact now that they no longer spend extra vertical space on redundant `Field Name` / `Type` / `Container` labels, every field row now keeps a visible `+` insert-after action beside delete instead of only the last row owning append, the field header action cluster now also exposes themed move up/down buttons that use the shared navigation arrow icons plus the standard disabled icon-button treatment, the missing vertical spacer below that action strip is back so the buttons do not sit flush against the first widget, container authoring now uses a typed editor (`Element` / `Array` / `Fixed Array` / `Pointer`) with conditional length or pointer-size inputs instead of a raw suffix string box, the fixed-array length input now runs through a validated unsigned-integer `DataValueBoxView` path so values like `-1` go invalid/red immediately instead of staying visually stable until save time, and the field data-type picker now opts into the shared selector’s explicit single-select mode so authored struct fields cannot accidentally accumulate multi-type / union-like selections. The create/edit takeover header itself is now a proper toolbar-style bar with the existing title text on the left plus themed `Cancel` / `Save` icon actions on the right, the old bottom action row is gone, and dirty tracking now compares the live draft against a stable takeover baseline instead of the per-frame mutable draft snapshot, so `Save` can actually enable once the user edits something while still requiring the draft to validate.
- need human verification: `DataValueBoxView` single-line editors now request the full inner text width instead of letting the single-line path keep a smaller natural width inside the right-aligned child layout. This should reduce the exaggerated left-side dead space that was especially obvious in wide SymbolStructEditor field-name inputs.
- need human verification: The symbol/type UX split now has a first reusable-type authoring window too. The old `Struct Viewer` is presented as `Details Viewer` in the dock/menu, a new `SymbolStructEditor` window can create/select/edit/delete reusable struct layouts, struct-layout edits persist through the opened project plus `project save` + privileged registry resync, field rows expose editable name/type/container columns, renaming a struct layout retargets any rooted symbols that referenced the old layout id, and the editor now uses shared GUI controls instead of raw egui widgets for its action buttons, string inputs, and selection rows, with icon-first add/delete affordances and themed foreground text/icon colors. The main window is list-first now: create/edit/delete route through centered takeover panels instead of paying for a permanently mounted right-side editor pane, so the list can use the full dock width in the normal state.
- need human verification: Memory Viewer attach-time page bootstrap is more robust now. The window registers its `ProcessChangedEvent` listener before the initial refresh, only auto-queries pages on real attach/open events instead of also on detach/clear, and a failed first post-attach memory-page query now gets one automatic retry so the viewer does not stay stuck empty until a later symbol-open forcibly seeks into it.
- need human verification: GUI Memory Viewer now has one lane-aware edit cursor instead of implicitly hex-only editing. Clicking/dragging in the left hex cells keeps the edit cursor on hex, clicking/dragging in the right string cells moves the edit cursor to the string lane, keyboard navigation preserves the active lane, and typed text now writes memory through the right-hand string lane without ever showing simultaneous left/right carets.
- need human verification: Memory Viewer selections are now capped to `2 MiB` in both GUI and TUI paths. Giant sparse/unallocated virtual pages should no longer let `Ctrl+A`, drag-extension, or cursor-extension create effectively page-sized selections that then force summary/materialization work across absurd ranges.
- need human verification: Memory Viewer address rows/header now reserve `128px` instead of `126px` for the address lane, which gives full 64-bit addresses the extra ~2px they needed to stop clipping against the divider.
- need human verification: `DataValueBoxView` no longer maps `Bool` interpretation mode to the string/display-text glyph. The value-format button now shows the dedicated boolean icon instead of the stray `S`-style icon.
- need human verification: Struct Viewer field presentation no longer lies about external-viewer buttons. `StructViewerFieldEditorKind` now distinguishes `MemoryViewerButton` from `CodeViewerButton`, the x86/x64 instruction-array case is decided up front in `StructViewerViewData::create_field_presentations()`, and `StructViewerEntryView` now just renders and dispatches the explicit target instead of sniffing the data type inside the widget.
- need human verification: Struct Viewer value-box rows now reserve a consistent trailing commit-slot width even when the field is read-only. Non-editable rows like address/module entries leave the same dead space an editable row would use for the checkmark button, so the data-value boxes stay vertically aligned instead of jutting wider on read-only rows.
- need human verification: `DataValueBoxView` now clips its inner text-edit child UI to the box’s actual inner rect before rendering. When a value box gets crushed narrow, preview/hint text should no longer bleed out past the left border instead of being clipped to the visible field.
- need human verification: The Element Scanner `Value` column header no longer stuffs the word `Value` inside the display-format combo box. The header now paints `Value` as real column text and keeps the format picker as a separate icon-only control aligned to the right side of the header, using the same `32px` footprint as a normal `DataValueBoxView` format button so it does not read like a shrunken special-case control.
- need human verification: Instruction plugin default-enable policy is now target-arch-aware instead of globally on: x86/x64 defaults on only for `x86` / `x86_64` builds, ARM/ARM64 defaults on only for `arm` / `aarch64` builds, and PowerPC defaults on only for `powerpc` / `powerpc64` builds. Non-matching built-in instruction plugins still ship and can still be enabled manually; only the default-on flag changed.
- need human verification: The x86 instruction plugin no longer carries a duplicate `X86InstructionMode` enum for plain 32/64-bit width tracking. It now uses the shared engine `Bitness` type through memory-operand parsing, opcode-candidate filtering, branch operand checks, and encoder/decoder setup, with local conversion to the `iced_x86` `32` / `64` API only at the call boundary.
- need human verification: Instruction data-type naming cleanup pass: the Rust-side built-in instruction data types now use explicit semantic names (`DataTypeInstructionX86`, `DataTypeInstructionX64`, `DataTypeInstructionArm`, `DataTypeInstructionArm64`, `DataTypeInstructionPowerPc32Be`) while preserving the short runtime/plugin IDs (`i_x86`, `i_x64`, `i_arm`, `i_arm64`, `i_ppc32be`). The x86 mnemonic lookup helper was also renamed from the vague `x86_opcode_index.rs` to `x86_opcode_candidate_index.rs` because it is specifically a cached mnemonic-to-`iced_x86::Code` candidate map used during operand lowering.
- need human verification: Project Explorer now exposes `Promote to Symbol` for address and pointer items, backed by a real `project-items promote-symbol` command. Address items promote directly to `module + offset` or absolute rooted symbols, pointer items resolve their current tail address at promotion time, and promoted pointer symbols keep their original pointer chain/evaluated path as metadata while showing up immediately in the project symbol store and Symbol Explorer.
- need human verification: Promoted symbols now also replace their acquisition items in place through `ProjectItemTypeSymbolRef`. The project item stores a rooted `symbol_key`, project-item preview/freeze/value-edit/memory/code/pointer-scan flows now resolve through the project symbol catalog, and address/pointer items stay acquisition tools instead of lingering as the long-term project representation after promotion.
- need human verification: Project Explorer symbol promotion now has conflict-aware takeover UX instead of silently suffixing keys forever. `project-items promote-symbol` now reuses an exact existing rooted symbol in place, reports same-key conflicts back through the shared response model, and the GUI shows an overwrite confirmation take-over before replacing an existing rooted symbol with the promoted one.
- need human verification: `ProjectItemTypeSymbolRef` can now be converted back in place without deleting the rooted symbol. The shared `project-items convert-symbol-ref` command now infers the natural reversion target per symbol (`pointer` when pointer-origin metadata exists, otherwise `address`), and Project Explorer shows one informed conversion action instead of exposing both address/pointer choices and making the user decide.
- need human verification: Rooted symbol management is now a first-class unprivileged command lane. `project_symbols list/create/rename/delete/update` mutates the project symbol store, persists to disk, and re-syncs the privileged symbol catalog, and Symbol Explorer now uses that shared path for rename/delete/type updates plus a new direct `New Rooted Symbol` authoring form for absolute-address and module+offset roots.
- need human verification: The GUI now has the first real split between `Symbol Table` and `Symbol Tree`. The old `Symbol Explorer` window is presented as `Symbol Tree`, a new flat `Symbol Table` window lists rooted symbols with filter/create/open/delete actions, both windows are wired into the `Windows` menu and default dock layout, and `Struct Viewer` focus ownership now has an explicit `SymbolTable` origin so the new table does not immediately lose detail focus back to the tree every frame.
- need human verification: CLI symbol coverage is no longer implicit-only. `squalr-cli` now parses both privileged and unprivileged command families, so `project`, `project_items`, and `project_symbols` commands can be driven directly from CLI, with basic response logging for project-item lists/promotions and rooted-symbol list/create/rename/delete flows.
- need human verification: Symbol Explorer now has the first real lazy symbol tree instead of a flat rooted-symbol list. Rooted symbols derive inline, nested-struct, and fixed-array children from their type layout plus locator, child nodes carry derived locators/full paths, expansion is view-local and non-persistent, and selected children can now be promoted directly into new rooted symbols through the shared `project_symbols create` workflow.
- need human verification: Symbol Explorer pointer children now resolve through a dedicated per-window virtual snapshot instead of fake offset math. Expanded pointer nodes issue `VirtualSnapshotQuery::Pointer` reads rooted at the pointer field locator, show a dereferenced `*` child when the target resolves, surface the evaluated pointer path/target address in details, and preserve full promoted type ids for arrays/pointers so derived-node promotion does not silently drop container information.
- need human verification: TUI Project Explorer now has a first rooted-symbol lane. In the project hierarchy, `p` promotes the selected project item to a rooted symbol and `s` switches into a symbol list mode that can browse rooted symbols, open them in the TUI memory viewer, refresh them, and delete them. CLI `project_symbols list` output is also richer now, including rooted-symbol metadata and authored type summaries.
- need human verification: TUI now has a separate `F7` Code workspace instead of forcing code browsing through the memory workspace. The new code viewer is virtual-snapshot-backed like the memory viewer, disassembles x86/x64 bytes through the shared instruction plugin based on the opened-process bitness, and Project Explorer rooted-symbol/project-item flows now expose a separate `v` open-in-code action alongside `o` open-in-memory.
- need human verification: TUI memory browsing is now much closer to the GUI lane. The memory workspace has a dedicated interpretation pane, byte-level keyboard navigation (`Left/Right`, `Up/Down`, `PgUp/PgDn`, `Ctrl+A`, `g`/`/` seek), in-place hex editing that writes back through the existing snapshot-driven path, and keyboard add-to-project from interpreted entries without opening a separate dialog.
- need human verification: GUI memory interpretation add buttons now visually disable when no project is open, using the same disabled button + tinted icon pattern as other toolbar controls, so the panel no longer invites add-to-project actions before a project exists.
- need human verification: GUI Plugins view content/details panes now clip correctly to their allocated rectangles. Long plugin lists and verbose detail text should no longer paint outside the docked pane when scrolled or resized.
- need human verification: `Symbol Explorer` is now discoverable from the GUI `Windows` toolbar menu in addition to the default dock layout. It was already a real docked window, but it had been omitted from the menu toggle list, which made it easy to miss or impossible to reopen from the menu if the tab was hidden.
- need human verification: `Symbol Explorer` styling/layout is much closer to the rest of the GUI now. Its action buttons use the shared themed button control, the view is back to a single-column tree instead of a split details layout, and list/type rows now force readable themed text instead of leaning entirely on default egui widget colors.
- need human verification: `Symbol Explorer` now uses a real left-pane header bar instead of the old custom in-view button strip. Rooted-symbol and derived-symbol actions are icon-based (`add`, edit, data/memory, CPU/code, trash) and live in the same `28px` themed header treatment used elsewhere in the codebase, while create mode swaps that bar to cancel/confirm actions; the old rooted/type count text was also removed from that bar because it was just noise.
- need human verification: `Symbol Explorer` rows and action placement are now closer to Project Explorer. The icon action bar now sits over the left symbol list instead of spanning the whole window, delete no longer uses a special danger-colored icon button, rooted/derived symbol rows render with project-style selected-row treatment, rooted rows now show `symbol_key` in the left identity cluster, and the right side now shows live preview values instead of locator/type filler text.
- need human verification: `Symbol Explorer` tree rows no longer print inline `[type]` preview text beside the symbol name. They now use the shared data-type icon converter for a proper type glyph beside the row label, keep live value previews on the right, and leave the full promoted type id plus locator in hover text instead of burning row width on them.
- need human verification: `Symbol Explorer` no longer renders any right-side details/property column at all. Rooted/derived selection now syncs the real shared `StructViewerViewData` so the actual Struct Viewer window is the only struct/details editor, rooted symbols expose editable `display_name` and `type` rows plus a read-only `symbol_key` row there, symbol-backed value fields can now write live memory from Struct Viewer, unnamed value fields fall back to neutral `value` / `value_N` labels instead of reusing the display name as a fake key, inline rename now cancels on outside click or `Escape`, and rooted-symbol delete/create now use centered Symbol Explorer take-overs instead of a side panel.
- need human verification: Struct Viewer focus ownership is now explicit across Project Explorer and Symbol Explorer. `StructViewerViewData` stores a shared focus target identity (`ProjectHierarchy` selected item paths or `SymbolExplorer` selection key), both explorers publish that owner when they actively move Struct Viewer focus, and both panes now derive their visible selected-row highlight from the shared focus target instead of trying to clear each other's local selection state. Symbol Explorer also still suppresses its old auto-select-first-rooted-symbol behavior while Project Explorer owns the Struct Viewer, so only the pane currently driving Struct Viewer details appears visually selected.
- need human verification: Project Explorer now has an internal project-item clipboard with `Cut`, `Copy`, `Paste`, and `Delete` in the item context menu plus `Ctrl+X/C/V` keyboard shortcuts. Cut no longer deletes immediately; it ghosts the pending-move rows in the tree with disabled-style text/icon tint until paste completes, copy-paste uses a new `project-items duplicate` command, cut-paste reuses `project-items move`, and the shared popup rows now clip/ellipsize long labels instead of bleeding outside the context menu while the popup itself uses a real minimum width instead of a fixed wide panel.
- need human verification: Project Explorer project-item context menus now size from their actual action labels instead of always using one fixed width. The popup keeps a `160` px minimum, but actions like `Open in Memory Viewer` or `Convert to Source Item Type` can widen it only when their measured label width actually needs more room.
- need human verification: Project Explorer context menus no longer let egui popup `available_width()` balloon the menu back out to panel/screen width after pre-measuring labels. Popup containers now set both min/max width from the computed menu width, and shared `ToolbarMenuItemView` rows no longer consult ambient `available_width()` inside popup scopes, so the project-item menu width is owned by the popup measurement pass instead of being re-expanded by child rows.
- need human verification: Top toolbar/menu-bar dropdowns now also size from their actual row layout instead of a too-small padding estimate. Shared `ToolbarHeaderItemView` popups now use the same row-width math as `ToolbarMenuItemView`, with a `160px` floor, so menu-bar items like `Windows` / `Project` / `View` no longer truncate their labels inside narrow dropdowns.
- need human verification: The top toolbar no longer has a separate `Scans` dropdown just for pointer scanning. Scan windows now live under `Windows`, and that menu uses a few light dividers to group explorers, viewers, scanners, and app/tool panes without over-segmenting the list.
- need human verification: `File -> Export Project as Table...` is now a real action instead of dead text. It dispatches the shared `project export` command for the opened project, opens the export folder on desktop after success, and the menu row now disables itself when no project is open.
- need human verification: The top menu bar now has a `Help` menu with an `About` action. `About` opens an app-level take-over panel instead of a docked window or modal, temporarily hiding the dock root while it shows the product logo, user-facing version/platform/application info, a `squalr.com` link, and close actions that work with desktop and Android-friendly flows.
- need human verification: Project Explorer project-item context menus now group actions more intentionally: pointer/runtime/promote/convert actions first, then a divider, then `New Folder` / `New Address` / `New Pointer`, then a divider, then `Cut` / `Copy` / `Paste`, then another divider, then `Delete`.
- need human verification: Project Explorer project-item context menus now hide unavailable actions instead of filling the menu with disabled rows. Group dividers also collapse with their groups, so directory targets no longer open with a leading separator when the whole runtime/promote/convert section is irrelevant.
- need human verification: Project Explorer create menus now include `New Symbol Ref` alongside folder/address/pointer creation, backed by a real `symbol_ref` project-item create path instead of just promotion flows. The current icon is still a placeholder unknown icon until a dedicated symbol glyph is chosen.
- need human verification: Project Explorer paste targets are now item-aware instead of directory-only. Pasting onto a folder still inserts into that folder, but pasting onto a non-folder item now inserts after that item; cut-paste onto a sibling in the same folder now reorders in place instead of silently no-oping or trying to rename onto itself.
- need human verification: Project Explorer project-item context menus now show placeholder icons for `Cut`, `Copy`, and `Paste` using the shared unknown icon, and `Delete` now uses the shared trash-can icon.
- need human verification: Project Explorer pointer-scan context menu wording is now action-oriented and more accurate: direct address/symbol entries say `Open in Pointer Scan`, while pointer entries distinguish `Open Base Address in Pointer Scan` from `Open Resolved Address in Pointer Scan`.
- need human verification: Project Explorer project-item context menus now have a first real icon pass for the remaining common actions: `New Folder` uses the folder icon, `New Address` uses the `i32` icon, `New Pointer` uses the pointer-type icon, `Open in Code Viewer` uses the CPU icon, and `Open in Memory Viewer` uses the shared data icon.
- need human verification: The main shortcut-bar process quick select no longer uses a hardcoded `256` px dropdown width when the visible process names are much shorter. Its popup now keeps the combo-box floor width, measures the widest current process/detach label to grow only when needed, and the shared `ComboBoxView` popup container now clamps to that computed width so the dropdown stops rendering as an oversized panel.
- need human verification: Combo-box popups now reserve their border inset instead of giving the entire framed rect to scrollable content. This was most visible in the process quick select, where the zero-margin popup let the `ScrollArea` occupy the same pixels as the popup stroke, making the bottom border look short and letting the last visible rows feel like they could scroll past the border.
- need human verification: Combo-box popups no longer use egui's stock `Frame::popup` geometry in the process quick-select path. The combo dropdown now uses an explicit flat frame with the repo theme stroke/fill so the visible bottom edge, border, and content rect all come from the same rectangle instead of inheriting popup shadow/stroke behavior that appeared to make the bottom edge look lower than the border line.
- need human verification: Combo-box row content in scrollable dropdowns now respects the scroll viewport clip rect instead of clipping only to each row's local text bounds. This specifically targets the process quick select case where scrolled text/icons could still end up half outside the popup border even after the frame geometry was flattened.
- need human verification: Shared `StateLayer` hover/background drawing now clips to the current UI clip rect instead of painting its full bounds rect unconditionally. This matters in scrollable combo dropdowns because partially visible rows were still able to paint hover/background fills below the popup border even after the combo text/icon painters were clipped.
- need human verification: Combo-box popups no longer rely on a fake bottom inset to hide the process quick-select border seam. The shared combo popup now keeps its flat framed `Area` path, but locally sets `visuals.clip_rect_margin = 0` before rendering scrollable contents so egui `ScrollArea` stops intentionally expanding the child clip rect by 3 px past the popup border.
- need human verification: Deleting selected Element Scanner results now immediately clears the local row selection before the engine delete request goes out, so the results pane no longer keeps a stale highlight/freeze/edit target pointed at rows that are about to disappear.
- need human verification: Settings pages no longer bleed out of small panels. `SettingsView` now wraps the active tab content in a vertical scroll area, the Memory settings page stacks its group boxes vertically instead of using the old cramped 2x2 layout, and reset/default docking now places `Settings` as a sibling tab with `Struct Viewer` while keeping `Struct Viewer` as the active tab. Reset/default docking also now shows all registered dock windows by default instead of seeding several tabs hidden, and `Symbol Explorer` shares the scan tab group with `Element Scanner` / `Pointer Scanner`.
- need human verification: The process-selector `Windowed` toggle now uses the themed checkbox/icon tint and themed foreground label color with the expected 4px label gap instead of relying on stock spacing/colors.
- need human verification: Pointer-scan browse tail work is lighter now because root-page materialization no longer flattens every static root candidate into a temporary vector before paginating. The `pointer_scan_target_ranges` direct bucket-lookup experiment was backed out after profiling concern, so bucket lookup is back on the prior bucketed binary-search path.
- need human verification: Pointer scanner start flows now surface an explicit completed status in the UI (`Pointer scan complete | ...`) instead of dropping straight from the in-flight message back to the generic session summary text.
- need human verification: Pointer scan kernel selection now follows the same repo-local pattern as element scans: a shared `PointerScanExecutionPlan` is built first, built-in pointer-scan planning rules choose a planned kernel, and discovery/validation both execute through `PointerScanDispatcher` instead of instantiating kernels directly from ad hoc thresholds in the search-kernel layer.
- need human verification: Pointer scan search-kernel semantics are now honest again. `PointerScanDispatcher` is the only place that switches on `PlannedPointerScanKernelKind`, it returns a static `PointerScanSearchKernel` strategy like element scan does, and the per-dispatch inputs now live in `PointerScanSearchKernelContext` instead of boxed kernel instances. `ScalarLinearPointerScanKernel`, `ScalarBinaryPointerScanKernel`, and `SimdLinearPointerScanKernel` are now true stateless strategies.
- need human verification: Pointer scan search-kernel code now maps planned kernel kinds to concrete modules without heap allocation. `pointer_scan_scalar_linear_search_kernel.rs`, `pointer_scan_scalar_binary_search_kernel.rs`, and `pointer_scan_simd_linear_search_kernel.rs` are the real strategy entry points, while the shared scalar byte-walk remains isolated as a private helper.
- need human verification: `pointer_scan_range_pass.rs` / `PointerScanRangePass` were renamed to `pointer_scan_candidate_collector.rs` / `PointerScanCandidateCollector`, because the file’s actual job is collecting pointer candidates from scan tasks with the selected kernel, not a vague generic “range pass.”
- need human verification: Pointer scan sharding is now natural only. The old per-worker byte chunking heuristics were removed, so discovery/validation now create one scan task per contiguous static or heap range and let Rayon schedule those ranges directly.
- need human verification: Pointer scan runtime state is now named and placed more honestly: shared persisted scan data and transient expand/paging caches now both live on `PointerScanResults`, with the cache fields marked `#[serde(skip, default)]` so they stay privileged-local and never bloat persistence or IPC. Pointer discovery/validation still share one natural-range task builder plus one shared candidate collector built around a single internal collected-candidate/level payload shape.

## Important Information
Append important discoveries. Compact regularly ( > ~40 lines, compact to 20 lines)

- The Struct Viewer external-viewer button policy used to be split awkwardly: view-data said `MemoryViewerButton`, then the row widget re-inspected the validation data type and sometimes silently turned that into a code-viewer action. This pass makes the presentation explicit with a real `CodeViewerButton` variant so the enum matches behavior, and the x86/x64 instruction-array case is now decided once in `StructViewerViewData` instead of ad hoc inside `StructViewerEntryView`. Reverified with `cargo fmt --all`, `cargo test -p squalr struct_viewer -- --nocapture`, and `cargo check -p squalr`.
- Instruction-family plugin default enablement is now a target-arch policy decision rather than a hardcoded global-on choice. Each built-in instruction plugin still registers everywhere, but its `PluginMetadata.is_enabled_by_default` flag now follows the compile target family so the default plugin set is less noisy on each platform while preserving manual opt-in for cross-ISA work. Reverified with `cargo fmt --all`, `cargo test -p squalr-plugin-instructions-x86`, `cargo test -p squalr-plugin-instructions-arm`, `cargo test -p squalr-plugin-instructions-powerpc`, and `cargo check -p squalr`.
- The x86 plugin was duplicating the engine’s `Bitness` concept under the misleading local name `X86InstructionMode`, even though it only represented 32-bit vs 64-bit width and had no x86-specific mode semantics. This pass removes that duplicate enum and threads shared `Bitness` through the x86 instruction-set/memory-operand/candidate-lowering code, keeping only tiny local conversions where `iced_x86` requires numeric bitness values. Reverified with `cargo fmt --all`, `cargo test -p squalr-plugin-instructions-x86`, and `cargo check -p squalr`.
- The old `DataTypeI*` names were leaking shorthand transport IDs into the Rust API surface, which made the type names read like abbreviations instead of concrete instruction-family data types. This pass keeps the short `i_*` string IDs exactly as before for registry/plugin compatibility, but renames the Rust structs/modules to explicit semantic names and updates the x86 helper filename to describe its actual job. Reverified with `cargo fmt --all`, `cargo test -p squalr-plugin-instructions-x86`, `cargo test -p squalr-plugin-instructions-arm`, `cargo test -p squalr-plugin-instructions-powerpc`, and `cargo check -p squalr`.
- Symbol-store planning is now captured in `docs/symbol_store.md` as an execution plan instead of a broad deep dive. The low-regret direction is: add named layout fields first, reuse pointer-scan pointer encodings in symbolic containers, add rooted project-owned symbols for `absolute` and `module + offset`, keep module names as strings for now, keep address/pointer items as promotion sources, derive children lazily, and keep external-tool quirks in metadata instead of hardcoding Ghidra/IDA-shaped core types.
- The symbol-store plan now also calls out the managed-runtime extension seam explicitly: virtual modules and data types remain the base, while CLR/JVM-style quirks should land later through a runtime/symbol-provider plugin that can contribute managed roots, resolution logic, and runtime-specific metadata without reshaping the core symbol model.
- The symbol-store plan now also settles the intended GUI shape: a dedicated Symbol Explorer should be the main symbol-authoring surface and should behave more like Project Explorer than Memory Viewer or Code Viewer. Memory/Code viewers stay page-oriented consumers of symbol labels with jump/promote actions, while Project Explorer keeps raw address/pointer acquisition items separate from the authored symbol tree.
- The symbol-store plan now states the project-model boundary explicitly: rooted symbols are their own stored symbolic records, not just decorated address/pointer items. Address and pointer items remain acquisition/workflow tools, and `Promote to Symbol` is the bridge that creates a real rooted symbol in the project symbol store.
- The symbol-store plan now gives the TUI an explicit track too: initial TUI symbol support should be tree-first in the project workspace, closer to the existing TUI project explorer than to page-oriented memory/code views. The current TUI memory viewer path is still relatively narrow and address-centric, so some TUI memory/code cleanup may be needed before deeper symbol-driven viewer integration feels good.
- The symbol-store plan now gives CLI explicit coverage too: symbol support should not be treated as "automatically covered" just because GUI/TUI use commands. When symbol workflows stay unprivileged-side, we should add dedicated unprivileged command paths so CLI can list/create/promote/inspect/rename/delete symbols without depending on GUI-only orchestration.
- First execution slice of the symbol-store plan is now in: `SymbolicFieldDefinition` supports optional stored field names with backward-compatible parsing/serde, `SymbolicStructDefinition::get_default_valued_struct()` now preserves those names when materializing `ValuedStruct` fields, and project symbol-catalog serialization tests now verify named fields survive a project-file round trip. Reverified with `cargo test -p squalr-engine-domain symbolic_field_definition -- --nocapture`, `cargo test -p squalr-engine-projects project_info_round_trip_preserves_project_symbol_catalog -- --nocapture`, and `cargo check -p squalr -p squalr-tui -p squalr-cli`.
- Symbol-store execution now has the next major slice in too: `Bitness` and `PointerScanPointerSize` moved down into the shared domain layer, `ContainerType` gained a new `Pointer(PointerScanPointerSize)` shape while still accepting legacy `Pointer32` / `Pointer64` serde values, `SymbolicFieldDefinition` now uses the shared pointer encoding model (including `u24be`-style pointers), `ProjectSymbolCatalog` now stores rooted symbols beside reusable layouts, and the GUI has a first dockable `Symbol Explorer` tab beside Project Explorer that can browse rooted symbols and symbol types and jump rooted locators into Memory/Code Viewer. Reverified with `cargo fmt --all`, `cargo test -p squalr-engine-domain symbolic_field_definition -- --nocapture`, `cargo test -p squalr-engine-domain container_type -- --nocapture`, `cargo test -p squalr-engine-domain pointer_scan_pointer_size -- --nocapture`, `cargo test -p squalr-engine-projects project_info_round_trip_preserves_rooted_symbols -- --nocapture`, `cargo test -p squalr symbol_explorer -- --nocapture`, `cargo test -p squalr project_explorer::project_hierarchy::project_hierarchy_view:: -- --nocapture`, and `cargo check -p squalr`.
- Symbol-store execution now has the promotion bridge too: there is a new unprivileged `project-items promote-symbol` command, a shared engine-side project-item symbol-resolution helper for address and pointer items, and a Project Explorer context action that promotes selected address/pointer items into rooted project symbols. Address items promote to module-relative or absolute locators, pointer items resolve their current tail address and store pointer provenance in metadata (`source.pointer_root`, offsets, pointer size, evaluated path) instead of forcing a pointer-chain locator into the phase-1 rooted-symbol model. Reverified with `cargo fmt --all`, `cargo test -p squalr-engine project_items_promote_symbol -- --nocapture`, `cargo test -p squalr project_hierarchy_view_data -- --nocapture`, and `cargo check -p squalr-engine -p squalr -p squalr-cli -p squalr-tui`.
- Symbol-backed project items now exist as a first-class built-in type: `ProjectItemTypeSymbolRef` stores a rooted `symbol_key`, `project-items promote-symbol` now swaps the original address/pointer item over to that symbol-ref in place, and the engine/UI read paths (preview refresh, freeze targets, struct-view edits, virtual snapshot queries, memory/code opens, pointer-scan context actions, and project-item icons) now dereference the project symbol catalog instead of assuming every long-lived project item still owns raw address/pointer fields. Reverified with `cargo fmt --all`, `cargo test -p squalr-engine-api project_item_type_symbol_ref -- --nocapture`, `cargo test -p squalr-engine project_items_ -- --nocapture`, `cargo test -p squalr project_hierarchy -- --nocapture`, and `cargo check -p squalr-engine -p squalr-engine-api -p squalr --quiet`.
- Symbol promotion no longer hides key collisions behind automatic suffixing in the project-item workflow. `project-items promote-symbol` now builds the canonical key from the display name, reuses an exact existing symbol when the locator/type/name already match, reports same-key conflicts back as structured response data when they do not, and overwrites that symbol only when the caller explicitly opts in. The GUI Project Explorer now uses that shared response to show an overwrite take-over, and the command tests cover exact reuse, conflict reporting, and explicit overwrite. Reverified with `cargo test -p squalr-engine project_items_promote_symbol -- --nocapture`, `cargo test -p squalr project_hierarchy -- --nocapture`, and `cargo check -p squalr-engine -p squalr`.
- `ProjectItemTypeSymbolRef` now has a real reverse conversion lane too, and it no longer asks the user to pick a target the symbol already implies. `project-items convert-symbol-ref` can infer the natural reversion target per symbol-ref (`pointer` when pointer-origin metadata exists, otherwise `address`), Project Explorer now shows a single smart conversion action with an informed label (`Convert to Pointer Item`, `Convert to Address Item`, or `Convert to Source Item Type` for mixed selections), and pointer-origin promotion also stores structured `source.pointer_root_module` / `source.pointer_root_offset` metadata so the round-trip does not depend purely on parsing display text. Reverified with `cargo test -p squalr-engine project_items_convert_symbol_ref -- --nocapture`, `cargo test -p squalr project_hierarchy -- --nocapture`, and `cargo check -p squalr-engine -p squalr`.
- Symbol-store execution now has a dedicated rooted-symbol management lane too: `project_symbols list/create/rename/delete` exists end-to-end in `squalr-engine-api` and `squalr-engine`, with shared symbol-key generation, project save + privileged catalog sync, and engine tests covering create/list/rename/delete persistence behavior. The GUI Symbol Explorer now uses those commands for rename/delete and also has a direct `New Rooted Symbol` details form that authors absolute-address or module+offset roots without needing a prior address/pointer item. Reverified with `cargo test -p squalr-engine project_symbols -- --nocapture`, `cargo test -p squalr symbol_explorer -- --nocapture`, and `cargo check -p squalr -p squalr-engine`.
- CLI is not “automatically covered” by the GUI/TUI command work in this repo because `squalr-cli` was still parsing only `PrivilegedCommand`. This pass adds a mirrored `dispatch_unprivileged_command` path on `EngineUnprivilegedState`, teaches the CLI parser to fall back from privileged to unprivileged command enums, adds underscore aliases like `project_symbols` / `project_items` on the unprivileged command family, and routes unprivileged responses through basic CLI handlers so symbol/project authoring commands are actually reachable from CLI. Reverified with `cargo test -p squalr-cli -- --nocapture` and `cargo check -p squalr-engine-api -p squalr-engine-session -p squalr-cli`.
- Audit against `docs/symbol_store.md` now says the major completed areas are: named layout fields, shared pointer/container encoding, rooted symbol persistence, promote-to-symbol, GUI rooted-symbol authoring, CLI symbol command coverage, and the first lazy inline child tree. The largest remaining plan gaps are pointer-child resolution on demand, a first TUI symbol browser, lightweight symbol labels/jumps in Memory/Code Viewer, and the longer-term privileged transport cleanup.
- Symbol Explorer now derives inline children rather than storing a child graph: the `symbol_tree_entry` builder resolves rooted symbol type ids, nested project layouts, inline field definitions, fixed arrays, and snapshot-resolved pointer targets into non-persistent tree nodes with stable view keys, derived locators, and full child paths. Child nodes can be browsed, opened in Memory/Code Viewer, and promoted into rooted symbols without persisting a separate child graph. Reverified with `cargo fmt --all`, `cargo test -p squalr symbol_explorer -- --nocapture`, and `cargo check -p squalr -p squalr-cli -p squalr-engine -p squalr-tui`.
- Audit against `docs/symbol_store.md` now says the major completed areas are: named layout fields, shared pointer/container encoding, rooted symbol persistence, promote-to-symbol, GUI rooted-symbol authoring, CLI symbol command coverage, lazy inline child trees, and pointer-child resolution on demand. The largest remaining plan gaps are the first TUI symbol browser, lightweight symbol labels/jumps in Memory/Code Viewer, and the longer-term privileged transport cleanup.
- Symbol Explorer pointer nodes now go through the existing virtual-snapshot system instead of one-off direct reads: the view does a structural tree pass, requests a dedicated `symbol_explorer_pointer_children` snapshot for expanded pointer nodes, then rebuilds the tree with resolved pointer targets as dereferenced `*` children. This keeps pointer-child expansion aligned with the current session-owned read/cache model, and it also fixed a promotion bug by preserving full derived type ids like `u16[2]` or `player*(u64)` instead of dropping the container portion when promoting tree nodes. Reverified with `cargo fmt --all`, `cargo test -p squalr symbol_explorer -- --nocapture`, and `cargo check -p squalr -p squalr-cli -p squalr-engine -p squalr-tui`.
- Audit against `docs/symbol_store.md` now says the major completed areas are: named layout fields, shared pointer/container encoding, rooted symbol persistence, promote-to-symbol, GUI rooted-symbol authoring, CLI symbol command coverage, lazy inline child trees, pointer-child resolution on demand, and a first TUI rooted-symbol browser. The largest remaining plan gaps are lightweight symbol labels/jumps in Memory/Code Viewer, a deeper TUI symbol-details/struct integration pass, and the longer-term privileged transport cleanup.
- The first TUI symbol pass now lives inside the existing Project Explorer pane instead of adding a brand-new workspace pane: when a project is open, `s` flips Project Explorer into a rooted-symbol list mode, `o` opens the selected rooted symbol in the TUI memory viewer, and `x` deletes it through `project_symbols delete`. The project-item tree also got a matching `p` hotkey for `project_items promote-symbol`, so the TUI now has an end-to-end acquisition-to-symbol workflow that stays on the shared unprivileged command path. CLI `project_symbols list` output now also prints rooted-symbol metadata and authored type summaries so the command is more useful for inspection scripts. Reverified with `cargo fmt --all`, `cargo test -p squalr-tui project_explorer -- --nocapture`, `cargo test -p squalr-cli -- --nocapture`, and `cargo check -p squalr-tui -p squalr-cli`.
- TUI now also has a distinct code-browsing lane instead of making memory do double duty: `F7` switches to a new `Code Workspace`, the pane reads bytes through its own virtual snapshot and decodes them into instruction rows with the shared x86-family instruction plugin, and Project Explorer uses `o` for memory vs `v` for code when opening selected address items or rooted symbols. This keeps memory/code as separate full-screen destinations in TUI without committing to a larger workspace-switcher design yet. Reverified with `cargo fmt --all`, `cargo check -p squalr-tui`, and `cargo test -p squalr-tui -- --nocapture`.
- TUI memory browsing now uses a real byte-selection model instead of a row-only cursor: the memory pane tracks a selected byte range plus hex-edit cursor, renders byte-level markers inline in the hex grid, supports quick seek input (`g` or `/`), range extension with `Shift` + arrows/page jumps, page-wide selection with `Ctrl+A`, and write-through hex editing via `MemoryWriteRequest`. The new interpretation pane stays in the same workspace, rebuilds from the current selection summary, and can add typed address items into the active project through the shared unprivileged project-item commands. Reverified with `cargo fmt --all`, `cargo test -p squalr-tui memory_viewer -- --nocapture`, `cargo test -p squalr-tui memory_interpretation -- --nocapture`, and `cargo check -p squalr-tui -p squalr`.
- GUI memory interpretation add buttons now respect project-open state instead of always appearing actionable. The interpretation panel reads `ProjectHierarchyViewData.opened_project_info`, disables the add button when no project is open, and tints the icon with the same enabled/disabled treatment used by other icon-button toolbars. Reverified with `cargo test -p squalr memory_viewer_interpretation_panel_view -- --nocapture` and `cargo check -p squalr`.
- `Symbol Explorer` was already wired into `MainWindowView` and the default dock layout beside `Project Explorer`, but it had been missed in `MainToolbarView` under `Windows`. The menu now exposes it like the other docked windows, so hidden layouts can reopen it without relying on the default tab group. Reverified with `cargo fmt --all` and `cargo check -p squalr`.
- Symbol Explorer had drifted into a mostly stock-egui surface even though the rest of the GUI uses themed controls and explicit foreground colors. This pass moves the obvious action surfaces back onto the shared button widget, wraps detail content in themed group boxes, makes the details panel scrollable, and forces readable themed text colors for symbol/type rows instead of relying on default widget visuals. Reverified with `cargo fmt --all` and `cargo check -p squalr`.
- The next Symbol Explorer polish pass pushes the repo conventions further: rooted-symbol display-name editing now uses `DataValueBoxView` with `string_utf8`, the window gained a real icon-based top action bar for create/apply/promote/open/delete flows, delete now uses a small take-over confirmation state instead of an immediate danger button, and the details group boxes explicitly request full panel width so they stop collapsing around short content. The process-selector `Windowed` checkbox also now uses themed foreground tinting and the expected 4px label spacing. Reverified with `cargo fmt --all`, `cargo test -p squalr symbol_explorer -- --nocapture`, and `cargo check -p squalr`.
- Symbol Explorer is back to the intended shape: one tree pane, no embedded property/details editor, and no right column at all. The temporary local details renderer was removed, the hidden Struct Viewer sync key now includes display name/type so rooted/derived selection refreshes the real shared `StructViewerViewData` when symbol metadata changes, and the actual Struct Viewer window is once again the only place symbol/property structs get rendered or edited. Rooted-symbol focus now prepends editable `display_name` and `type` rows plus a read-only `symbol_key`, the Struct Viewer commit callback routes metadata edits through `project_symbols rename/update`, and symbol-backed value rows can now dispatch live `MemoryWriteRequest`s from Struct Viewer instead of being dead previews. Symbol tree rows also moved further toward the Project Explorer shape by showing `symbol_key` on the left and live preview values on the right while keeping locator/type details in hover text. Create-rooted-symbol moved to a centered take-over inside Symbol Explorer, delete stayed take-over-based, and the left-pane action strip remains a dedicated `symbol_explorer_toolbar_view.rs` header-bar component. Reverified with `cargo fmt --all`, `cargo test -p squalr-engine project_symbols_update -- --nocapture`, `cargo test -p squalr symbol_explorer -- --nocapture`, and `cargo check -p squalr -p squalr-engine`.
- Rooted symbol keys are effectively immutable in the current command surface. `project-symbols rename` targets an existing `symbol_key` and only updates `display_name`; there is no shared command yet to rekey a rooted symbol and rewrite any `ProjectItemTypeSymbolRef` backlinks that point at that key. Any future editable-key UX should ride on a dedicated engine command instead of pretending the current rename path handles it.
- Symbol Explorer row rendering is now split into its own `symbol_tree_entry_view.rs` instead of hiding inside the giant main view. That new row view mirrors the Project Explorer row shape: selected-background/border styling, hover state layer, arrow-hitbox expansion, and one-line `display name + type preview + locator` layout. The action bar also moved to the top of the left list pane so it feels tied to the active symbol selection instead of the whole split view. Reverified with `cargo fmt --all`, `cargo test -p squalr symbol_explorer -- --nocapture`, and `cargo check -p squalr`.
- Project Explorer now has a dedicated internal clipboard lane for project items instead of leaning on the OS clipboard. The GUI context menu gained `Cut`, `Copy`, and `Paste`; `Ctrl+X/C/V` now target the selected project items; cut rows are rendered ghosted in the hierarchy until pasted; copy-paste goes through a new shared `project-items duplicate` command in the engine; and cut-paste reuses `project-items move`. The earlier “make the menu wider” workaround was backed out; the real overflow fix now lives in the row rendering plus Project Explorer’s popup sizing, so long labels clip cleanly and the popup only widens past its minimum when the current actions need it. Reverified with `cargo fmt --all`, `cargo test -p squalr project_hierarchy -- --nocapture`, and `cargo check -p squalr-engine-api -p squalr-engine -p squalr-cli -p squalr`.
- Project Explorer paste semantics are now explicitly target-shaped instead of treating every target as “resolve parent directory and append.” The view-data now distinguishes folder targets (`paste into`) from item targets (`paste after`), copy-after uses the duplicate response’s real emitted paths before issuing a sibling reorder, and cut-after can reorder within the same parent without a move request while mixed-parent cuts still do move-then-reorder. Reverified with `cargo fmt --all`, `cargo test -p squalr project_hierarchy_view_data -- --nocapture`, and `cargo check -p squalr`.
- Project Explorer context menus were still rendering several type-specific actions as disabled placeholders, which made folder menus especially noisy and could leave a stray divider at the top once those rows were conceptually “empty.” The menu builder now only includes visible action labels/actions for width measurement and rendering, and separator placement is keyed off whether the adjacent groups actually rendered. Reverified with `cargo fmt --all`, `cargo test -p squalr project_hierarchy -- --nocapture`, and `cargo check -p squalr`.
- Project Explorer’s create lane no longer stops at folder/address/pointer. `ProjectHierarchyCreateItemKind` and the shared create-request builder now include `SymbolRef`, the add/context menus show `New Symbol Ref`, and the engine `project-items create` executor now persists a blank-key `ProjectItemTypeSymbolRef` placeholder item so symbol refs can be authored directly in the project tree instead of only appearing via promotion. Reverified with `cargo fmt --all`, `cargo test -p squalr project_hierarchy -- --nocapture`, and `cargo check -p squalr -p squalr-engine`.
- Project Explorer context menus now size from their current labels instead of a fixed panel width. The popup keeps a `160` px minimum, measures the widest action label before rendering, and only expands past that floor when a longer action genuinely needs more room. The shared `ToolbarMenuItemView` rows now also treat their configured width as a minimum, so a later long action cannot get stuck inside an earlier short row width. Reverified with `cargo fmt --all`, `cargo test -p squalr toolbar_menu_item_view -- --nocapture`, `cargo test -p squalr project_hierarchy -- --nocapture`, and `cargo check -p squalr`.
- The same width-regression cleanup had also nicked the top menu-bar dropdowns because `ToolbarHeaderItemView` was still estimating popup width with its own incomplete padding math while `ToolbarMenuItemView` reserved a larger icon/text gutter internally. The toolbar popups now share one `row_width_from_text_width()` helper with a `160` px floor, so top-level menus grow to fit their labels again instead of truncating everything in a too-narrow popup. Reverified with `cargo fmt --all`, `cargo test -p squalr toolbar_menu_item_view -- --nocapture`, and `cargo check -p squalr`.
- The top toolbar also had an odd IA split where `Scans` existed as a separate top-level dropdown even though the main toolbar already has a `Windows` menu for docked panes, and the `Scans` menu only exposed pointer scanning. `MainToolbarView` now removes that duplicate top-level header and uses a few separators inside `Windows` to group explorers, viewers, scanners, and app/tool panes instead. Reverified with `cargo fmt --all`, `cargo test -p squalr main_toolbar_view -- --nocapture`, and `cargo check -p squalr`.
- The `File` menu had another dead row: `Export Project as Table...` already had a real engine command behind it (`project export` serializes the opened project to `export/<project>.json`), but the toolbar never dispatched it. The shared toolbar menu rows now support a proper disabled state, `MainToolbarView` wires export to `ProjectExportRequest { open_export_folder: true }`, and the row disables when `ProjectManager` has no opened project. Reverified with `cargo fmt --all`, `cargo test -p squalr main_toolbar_view -- --nocapture`, and `cargo check -p squalr`.
- The main window had no app-level non-modal take-over lane, so `Help -> About` could not be implemented cleanly as anything other than a docked window or a blocking popup. `MainWindowView` now owns a small `MainWindowTakeOverState`, `MainToolbarView` exposes a new `Help` menu with `About`, and the new `AboutTakeOverView` replaces the dock root while active with a centered product card showing the logo, version plus user-facing application/platform/architecture info, `squalr.com`, and close actions. The view now claims the full takeover rect so the footer stays pinned instead of floating upward, and Escape also closes it. Reverified with `cargo fmt --all`, `cargo test -p squalr main_window -- --nocapture`, and `cargo check -p squalr`.
- The Memory Viewer could get stuck with no visible page data right after a basic attach, yet instantly recover if a project/symbol flow opened the viewer on a concrete address. The attach bootstrap now registers the `ProcessChangedEvent` listener before the initial page refresh, only triggers the post-change page query when a process is actually opened, and arms a one-shot retry if that first attach-driven `memory query` response comes back unsuccessful so the viewer does not stay wedged empty until a later explicit seek. Reverified with `cargo fmt --all`, `cargo check -p squalr`, and `cargo check -p squalr --tests`; targeted `cargo test -p squalr memory_viewer_view -- --nocapture` / `cargo test -p squalr memory_viewer_view_data -- --nocapture` both reached a Windows linker `LNK1201` PDB-write failure in this environment rather than a Rust compile/test failure.
- The GUI Memory Viewer had only one real edit path: any click in the right-hand text lane still left editing logically bound to the left hex bytes, so there was no way to put the cursor on the string interpretation itself or type directly there. The view/model now share a lane-aware edit state (`Hexadecimal` vs `String`), row hit-testing distinguishes which side the click/drag began on, arrow-key movement preserves the active lane, and the view draws exactly one caret on either the hex nibble cell or the right-hand string cell. Typed text now routes through the active lane and dispatches memory writes from the string side as well. The current string write codec intentionally stays aligned with the current one-byte ASCII-ish right-hand rendering, so this adds the cursor/write plumbing without pretending multi-byte/future string interpretations are solved yet. Reverified with `cargo fmt --all`, `cargo test -p squalr memory_viewer_view_data -- --nocapture`, `cargo test -p squalr memory_viewer_view -- --nocapture`, `cargo check -p squalr`, and `cargo check -p squalr --tests`.
- Giant virtual pages had an ugly memory-viewer failure mode: rendering is viewported, but full-page selection paths (`Ctrl+A`, drag extension, keyboard extension) could still hand selection summary / interpretation code an effectively unbounded byte range. Both GUI and TUI memory viewers now clamp selections to `2 MiB` while still keeping the anchor/cursor on-page, so opening a massive sparse region no longer implies page-sized `selected_bytes` materialization. Reverified with `cargo fmt --all`, `cargo test -p squalr memory_viewer_view_data -- --nocapture`, `cargo test -p squalr-tui pane_state -- --nocapture`, `cargo check -p squalr`, `cargo check -p squalr --tests`, `cargo check -p squalr-tui`, and `cargo check -p squalr-tui --tests`.
- Struct Viewer value rows were still slightly ragged because editable `DataValueBoxView` rows reserved trailing width for the commit checkmark, but read-only value rows reused that space for the text box. `StructViewerEntryView` now reserves the same trailing commit-slot width for all value-box rows, mirroring the selector rows that already kept a fixed trailing slot, so fields like address/module align with editable values instead of expanding wider. Reverified with `cargo fmt --all`, `cargo test -p squalr struct_viewer -- --nocapture`, and `cargo check -p squalr`.
- The Memory Viewer address lane was just a hair too narrow for full 64-bit addresses, so the trailing hex digit could clip against the divider. `MemoryViewerView` now reserves `128px` instead of `126px` for the address column, and there is a tiny regression test documenting that width. Reverified with `cargo fmt --all`, `cargo test -p squalr memory_viewer_view -- --nocapture`, `cargo test -p squalr data_value_box -- --nocapture`, and `cargo check -p squalr`.
- `DataValueBoxView` was still mapping `AnonymousValueStringFormat::Bool` to the generic string/display icon, which is why the value-format button showed an `S`-style glyph instead of the boolean icon. The control now routes `Bool` through the dedicated bool icon handle, and there is a small unit test covering that icon-kind mapping. Reverified with `cargo fmt --all`, `cargo test -p squalr data_value_box -- --nocapture`, `cargo test -p squalr memory_viewer_view -- --nocapture`, and `cargo check -p squalr`.
- `DataValueBoxView` was still missing one paint boundary: the child `Ui` hosting its `TextEdit` inherited a broad clip rect, so when the control width collapsed the preview/hint text could still draw outside the left edge of the box. The control now intersects the text-edit child clip with the box’s inner text rect before rendering, and there is a small geometry test covering that clip helper. Reverified with `cargo fmt --all`, `cargo test -p squalr data_value_box -- --nocapture`, and `cargo check -p squalr`.
- The Element Scanner results header had a local misuse of `ComboBoxView`: the `Value` column name was being passed in as the combo label, so the display-format picker rendered the word `Value` inside the control. The header now owns that layout explicitly by painting `Value` as header text, keeping the display-format picker icon-only, right-aligning that picker within the value column, and matching its width to the normal `DataValueBoxView` format-button footprint (`32px`) so it still reads like the same control family. Small geometry tests cover the selector and text clip rect math. Reverified with `cargo fmt --all`, `cargo test -p squalr element_scanner_results_view -- --nocapture`, and `cargo check -p squalr`.
- The real egui trap in the oversized-menu regression was that shared popup rows were still doing `self.width.max(user_interface.available_width())` inside popup scopes. In an `Area`/popup that ambient available width can be far larger than the intended menu, so the child rows re-inflated the popup after the parent had already done the right label measurement work. The fix was to make popup width ownership explicit: Project Explorer still pre-measures the action labels and computes a minimum-or-content menu width, `ContextMenu`/toolbar popups now clamp both min/max width to that value, and `ToolbarMenuItemView` now renders to its configured row width instead of sampling ambient popup width. Reverified with `cargo fmt --all`, `cargo test -p squalr project_hierarchy -- --nocapture`, and `cargo check -p squalr`.
- Project Explorer project-item context menus now insert separators between each major action group. The order is now `{pointer/runtime/promote/convert}` -> separator -> `{New Folder, New Address, New Pointer}` -> separator -> `{Cut, Copy, Paste}` -> separator -> `{Delete}` so creation, clipboard, and destructive actions stop blending into the item-specific commands. Reverified with `cargo fmt --all`, `cargo test -p squalr project_hierarchy -- --nocapture`, and `cargo check -p squalr`.
- `ToolbarMenuItemView` now supports optional leading icons for non-checkable rows, and the Project Explorer item context menu uses that to give `Cut`, `Copy`, and `Paste` a temporary unknown-icon placeholder while wiring `Delete` to the shared trash-can icon. Reverified with `cargo fmt --all`, `cargo test -p squalr project_hierarchy -- --nocapture`, and `cargo check -p squalr`.
- Project Explorer pointer-scan context actions now use `Open ... in Pointer Scan` wording instead of the older noun-style `Pointer Scan ...` labels, which better matches what the action actually does from the context menu. Reverified with `cargo fmt --all`, `cargo test -p squalr project_hierarchy -- --nocapture`, and `cargo check -p squalr`.
- The Project Explorer item context menu now assigns concrete icons to the remaining create/runtime actions instead of leaving them text-only: folder for `New Folder`, blue `i32` blocks for `New Address`, the project pointer-type icon for `New Pointer`, CPU for `Open in Code Viewer`, and the shared purple data blocks for `Open in Memory Viewer`. Reverified with `cargo fmt --all`, `cargo test -p squalr project_hierarchy -- --nocapture`, and `cargo check -p squalr`.
- The process quick-select was a different bug path: it does not use `ContextMenu`/`ToolbarMenuItemView`, it uses the custom combo-box controls. The shortcut bar still had a literal `process_dropdown_list_width = 256.0`, so even after the popup-sizing cleanup it kept asking egui for a comically wide process dropdown. That pass now computes the width from the current process labels plus fixed row padding, keeps `224` px as the floor to match the closed control, and clamps the shared `ComboBoxView` popup to that measured width. Reverified with `cargo fmt --all`, `cargo test -p squalr main_shortcut_bar_view -- --nocapture`, and `cargo check -p squalr`.
- The follow-up geometry bug on the process quick select was a border/content rect mismatch inside the shared combo popup. `ComboBoxView` was still using `Frame::popup(...).inner_margin(Margin::ZERO)`, which meant a scrolling dropdown could render right up into the popup stroke area; visually the bottom border looked short, and the scroll viewport felt slightly taller than the bordered panel. The fix was to reserve the popup border width as the frame inner margin so scrollable content is clipped inside the visible border. Reverified with `cargo fmt --all`, `cargo test -p squalr main_shortcut_bar_view -- --nocapture`, and `cargo check -p squalr`.
- The remaining bottom-edge mismatch on the process quick select appears to be frame-level rather than row-level. Even after reserving border inset, the dropdown was still using `Frame::popup`, whose popup-specific geometry/shadow behavior did not visually align with the intended flat combo border. The combo popup now uses an explicit `Frame::new().fill(...).stroke(...).inner_margin(0).outer_margin(0)` so the stroke and fill share one exact rect. Reverified with `cargo fmt --all`, `cargo test -p squalr main_shortcut_bar_view -- --nocapture`, and `cargo check -p squalr`.
- The remaining content bleed was a row-level clip bug inside `ComboBoxItemView`. Its text painter was using `with_clip_rect(text_rectangle)` directly, which is fine for static lists but not for a scrolled viewport because the effective clip needs to be the intersection of the row bounds and the current UI clip rect. The combo rows now intersect their local content rects with `user_interface.clip_rect()` before drawing text/icons, so scrolled entries should no longer paint partly outside the popup border. Reverified with `cargo fmt --all`, `cargo test -p squalr main_shortcut_bar_view -- --nocapture`, and `cargo check -p squalr`.
- One more shared paint path was still bypassing the scroll viewport: `StateLayer` itself. It was drawing row hover/pressed fills and optional borders directly against `bounds_rect` after only checking `is_rect_visible`, which is not enough for partially clipped scroll rows. `StateLayer` now intersects its bounds with `user_interface.clip_rect()` before painting, so partially visible combo rows should stop bleeding hover/background pixels past the popup border. Reverified with `cargo fmt --all`, `cargo test -p squalr main_shortcut_bar_view -- --nocapture`, and `cargo check -p squalr`.
- The real cause of the process quick-select bottom-edge bleed appears to be egui itself: `ScrollArea` expands its content clip rect by `ui.visuals().clip_rect_margin`, and egui 0.33.3 defaults that margin to `3.0`. That matches the exact amount the temporary bottom-inset hack had to hide. The shared combo popup now drops the failed explicit-body-rect experiment and instead sets `visuals.clip_rect_margin = 0.0` locally before rendering scrollable contents, while keeping the flat frame and constrained `Area`. Reverified with `cargo fmt --all`, `cargo test -p squalr main_shortcut_bar_view -- --nocapture`, and `cargo check -p squalr`.
- Element Scanner result deletion was leaving selection state behind because `delete_selected_scan_results()` only gathered refs and dispatched the engine request. The local selection model is just `selection_index_start/end`, so clearing those immediately on delete keeps the action bar and row highlight from targeting disappearing results while the existing `ScanResultsUpdatedEvent`-driven requery catches up. Reverified with `cargo fmt --all`, `cargo test -p squalr element_scanner_results_view_data -- --nocapture`, and `cargo check -p squalr`.
- Settings polish is a mix of behavior and layout: the main settings view now scrolls when docked small, the Memory tab now stacks all four group boxes vertically instead of a 2x2 grid, and the default reset layout was changed so `Settings` shares a tab group with `Struct Viewer` while keeping `Struct Viewer` selected by default. Reset/default docking also now seeds all registered windows visible instead of hiding Process Selector, Memory Viewer, Code Viewer, Plugins, or Symbol Explorer behind hidden tabs. `Symbol Explorer` now shares the scan tab group with `Element Scanner` and `Pointer Scanner`. Reverified with `cargo fmt --all`, `cargo check -p squalr`, and `cargo test -p squalr dockable_window_settings -- --nocapture`.
- Pointer scanning in Rust was already using multiple range-search kernels (`SIMD Linear`, `Scalar Linear`, `Scalar Binary`), but the selection logic is still much more ad hoc than the old C# pointer-search-kernel factory. The direct bucket-lookup experiment was reverted, the root-page browse optimization stayed, and the next real cleanup should be an explicit pointer-search kernel selector/factory plus a clearer pointer-scan planning layer instead of sprinkling thresholds through helper files. Reverified with `cargo fmt --all`, `cargo test -p squalr-engine-api pointer_scan_results -- --nocapture`, `cargo test -p squalr-engine-scanning pointer_scan_target_ranges -- --nocapture`, `cargo test -p squalr-engine-scanning pointer_scan_validator -- --nocapture`, `cargo test -p squalr pointer_scanner_view_data -- --nocapture`, and `cargo check -p squalr`.
- The pointer-scan browse tail is still partly a GUI policy issue too: `PointerScannerViewData` eagerly queues root page 0 right after summary/start/validate responses, so some post-scan work still begins immediately even before the user paginates. That was left unchanged in this pass because deferring it cleanly needs a small follow-up in the results view to request the first page lazily instead of assuming it was prequeued.
- Two tiny pointer-scan helper files were adding indirection without earning it: `pointer_scan_target_range_bucket.rs` and `pointer_validation_level_log_context.rs` were both folded back into their owning modules so the code reads closer to the actual algorithm and there are fewer mystery one-struct files in the pointer-scan tree.
- Pointer scan kernel planning now follows the same high-level shape as element scans instead of hiding policy in low-level search files. The shared API now has `PointerScanExecutionPlan`, `PlannedPointerScanKernelKind`, `PointerScanPlanningRule`, and `PointerScanRuleRegistry`, while the scanning crate has a `PointerScanDispatcher` that builds a plan from rules and returns the matching concrete kernel implementation for discovery/validation. The current built-in rule still preserves the existing small/medium/large frontier thresholds, but those thresholds now live in a named planning rule (`RuleMapSearchKernel`) instead of a low-level kernel helper file. Reverified with `cargo fmt --all`, `cargo test -p squalr-engine-api pointer_scan -- --nocapture`, `cargo test -p squalr-engine-scanning pointer_scan_dispatcher -- --nocapture`, `cargo test -p squalr-engine-scanning pointer_scan_validator -- --nocapture`, and `cargo check -p squalr`.
- Pointer scan range-search kernels now line up semantically with their names and with the element-scan dispatcher shape: the dispatcher owns the enum switch, returns a static `PointerScanSearchKernel` strategy, and the selected strategy receives a `PointerScanSearchKernelContext` containing the target ranges and pointer size. The shared scalar pointer-byte walk still exists, but only as a private helper used by the concrete strategies. Reverified with `cargo fmt --all`, `cargo test -p squalr-engine-scanning pointer_scan_dispatcher -- --nocapture`, and `cargo check -p squalr-engine-scanning`.
- Pointer-scan naming cleanup continues: `pointer_scan_range_pass.rs` / `PointerScanRangePass` were renamed to `pointer_scan_candidate_collector.rs` / `PointerScanCandidateCollector` so the file now reads like what it actually does. This was a comprehension-only rename with no behavior change. Reverified with `cargo fmt --all`, `cargo test -p squalr-engine-scanning pointer_scan_dispatcher -- --nocapture`, and `cargo check -p squalr-engine-scanning`.
- Pointer scan task shaping is now much simpler too: the old `*_TARGET_TASKS_PER_WORKER` / byte-chunk-size heuristics were removed from discovery and validation, and task builders now keep only the natural semantic sharding boundaries (contiguous heap/static ranges with pointer-width overlap at the end). That keeps the domain-required slicing while leaving worker balancing to Rayon instead of second-guessing it with extra top-level tuning. Reverified with `cargo test -p squalr-engine-scanning pointer_scan_level_collector -- --nocapture`, `cargo test -p squalr-engine-scanning pointer_scan_validator -- --nocapture`, `cargo test -p squalr-engine-scanning pointer_scan_dispatcher -- --nocapture`, and `cargo check -p squalr`.
- Pointer scan structure got a cohesion cleanup pass: shared persisted scan data and transient expand/paging caches now both live directly on `PointerScanResults`, with cache fields skipped from serde so they remain rebuildable privileged-local state rather than persisted or IPC-shuttled payload. The separate `PointerScanResultsMaterializer` object and privileged-state slot are gone, expansion/reset now mutate the active results directly, and discovery/validation still share one natural-range task builder plus the shared `PointerScanCandidateCollector` around a single internal `PointerScanCollectedCandidate` / `PointerScanCollectedLevel` shape. Reverified with `cargo fmt --all`, `cargo test -p squalr-engine-api pointer_scan_results -- --nocapture`, `cargo test -p squalr-engine-scanning pointer_scan_executor_task -- --nocapture`, `cargo test -p squalr-engine-scanning pointer_scan_validator -- --nocapture`, `cargo test -p squalr-engine pointer_scan_ -- --nocapture`, and `cargo check -p squalr-engine-api -p squalr-engine-scanning -p squalr-engine-session -p squalr-engine`.
- The current ownership split is directionally correct for symbol authoring: projects persist symbols unprivileged-side, project open/close syncs them into the privileged symbol registry for execution-time reads/writes, and the unprivileged side also keeps local-project resolution for struct layouts. The main scalability hazard is that privileged registry refresh is still whole-catalog by generation, so large authored symbol stores would be expensive over Android or other IPC-heavy deployments.
- Virtual modules already have the right extension seam through `MemoryViewInstance` (`get_modules`, `address_to_module`, `resolve_module_address`, pointer-scan region overrides), and the Dolphin plugin proves we can label guest memory as module-like roots. A future symbol store should reference stable module ids from that layer instead of inventing a second virtual-module abstraction.
- Weird architecture support is inconsistent today: pointer scans already model `u24` / `u24be` / `u32` / `u32be` / `u64` / `u64be`, but symbolic field/container modeling still hardcodes only `Pointer32` and `Pointer64`. Unifying pointer/container encoding is a prerequisite before a symbol store can safely support unusual targets.
- The best low-regret implementation order looks like: generalize pointer/container encoding, add rooted symbol instances beside reusable type/layout definitions, keep the full authored symbol store unprivileged-owned, send only compact execution-oriented symbol data to the privileged side, and build scan/address labeling from exact-address and inline-field matches before attempting any full reverse pointer-map style symbol resolution.
- `docs/symbols.md` now captures the Ghidra-inspired UX split we likely want next: `Symbol Table` for flat maintenance, `Symbol Tree` for hierarchy/navigation, `Data Type Manager` for reusable types, and more symbol/type authoring directly from `Code Viewer` / `Memory Viewer`. This is intended as the UX companion to `docs/symbol_store.md`, not a replacement for the current backend/store plan.

