# Agentic Current Task (Readonly)
Our current task, from `README.md`, is:
`pr/TODO`

# Notes from Owner (Readonly Section)
- Assume any unstaged/uncomitted file changes are from a previous iteration (or if this file, probably the human author giving guidance), and can be kept if they look good. Do not ask me about them.
- Assume any connected android devices are rooted, and assume MacOS has SIP disabled.
- You don't get to declare things as fixed. Only "need human verification".

## Current Tasklist (ordered)
(Remove as completed, add remaining concrete tasks. If no tasks, audit the GUI project against the TUI and look for gaps in functionality. Note that many of the mouse or drag heavy functionality are not really the primary UX, so some UX judgement calls are required).

- need human verification: The Memory Viewer interpretation panel now uses the same `background_panel` fill as the main memory viewer surface and no longer draws its own border, so it reads as one continuous surface instead of a boxed-in sidecar.
- need human verification: Code Viewer now labels the jump-arrow gutter as `Jumps` in the header instead of leaving it blank, while keeping the existing breakpoint/address/bytes/assembly column layout intact.
- need human verification: Code Viewer and Memory Viewer headers now match the scan-results styling more closely: larger header font, normal foreground color, and a thin horizontal separator under the header row. Code Viewer selection changes now also cancel any in-progress inline instruction edit, and the shared warning theme color was darkened to `#CC6633` with the underfill message rewritten to say `code that does nothing` instead of `NOPs`.
- need human verification: Code Viewer and Memory Viewer no longer show the old attach/unreadable/no-decoded empty-state warnings in their main content panes; idle panels now stay visually quiet until pages are available, while active refresh still uses the existing spinner state.
- need human verification: The scanner data-type compatibility gating now treats an empty selection as having no compatibility family yet, so all data types become available again when the current selection is cleared instead of being biased by the stale visible data-value format. Code Viewer instruction overwrite/fill warnings also now use roomier rows, a wider asm warning area, and real themed action buttons (`Fill + Write` in blue, `Write Anyway` in warning amber).
- need human verification: Code Viewer and Memory Viewer now render a dedicated column-header row directly under the top toolbar, aligned to the live column geometry instead of hard-coded text offsets. Code Viewer shows `BP | Address | Bytes | Assembly` with the branch-arrow gutter intentionally unlabeled, and Memory Viewer shows `Address | Bytes | String UTF-8` over the left content pane while leaving the interpretation panel unchanged.
- need human verification: Code Viewer and Memory Viewer now render real vertical column separators through the scroll surface instead of free-floating text columns. Address separation is treated as a fixed visual divider, while the content divider is now draggable and persisted per viewer (`bytes <-> asm` in Code Viewer, `hex <-> ascii` in Memory Viewer). Code Viewer’s inline instruction editor and warning row now align to the live instruction text column instead of the old hard-coded byte-width layout, and Memory Viewer clips hidden hex/ascii cells correctly when the divider is moved so columns can be intentionally occluded without painting through each other.
- need human verification: Code Viewer and Memory Viewer no longer auto-follow local selection clicks the same way, both now clear the current selection on `Esc`, and both now expose a single-row header address `DataValueBox` plus apply arrow that seeks to the requested address and clamps to the nearest available page/address when the exact target is not present. The address inputs now default to hex-style entry, row addresses in both viewers render in the theme hexadecimal color, the existing bottom footer navigation bars remain in place, and Code Viewer now supports contiguous multi-select, right-click `Add Instruction(s) to Project`, x86/x64 instruction item routing from project/Struct Viewer into Code Viewer, and inline instruction editing through the shared `DataValueBox` instead of an ad hoc row editor. Instruction edits now open from double-click or context-menu `Edit Instruction(s)`, use single-line assembly text in Code Viewer, replace the instruction text in-place within the edited row instead of spawning a separate row above it, validate against `i_x86` / `i_x64`, and keep the exact-write, NOP-fill, and overwrite warning flows.
- need human verification: Follow-up viewer polish pass: Code Viewer now draws extra facade separators between the breakpoint gutter, jump gutter, and address column so those lanes read more like explicit columns, both viewers give the address text some right-side breathing room before the facade bar, the default Code Viewer bytes column starts noticeably narrower, and the inline instruction editor now stays left-aligned with a capped width instead of stretching across the whole asm column.
- need human verification: Follow-up edit-row height pass: the Code Viewer row currently under instruction edit is now taller than a normal disassembly row so the shared `DataValueBox` fits inside the row cleanly instead of feeling vertically cramped.
- need human verification: The element scanner now renders `i_*` instruction inputs as taller multiline `DataValueBox` rows instead of cramped single-line value fields, so multi-instruction assembly can be entered directly with newline-separated text in addition to `;` separators. The instruction scan hint text now calls this out explicitly.
- need human verification: The element scanner data-type selector now enforces format-compatibility families when multi-selecting scan types. It grays out types whose supported `AnonymousValueStringFormat` set does not match the currently selected family, which keeps incompatible mixes like scalar numeric types plus instruction/string-only types from being selected together in one scan.
- need human verification: The default dock layout now groups `Output`, `Memory Viewer`, `Code Viewer`, and `Plugins` into the same tab cluster, and missing-window repair reattaches those auxiliary/runtime-inspection windows there for layouts that do not already contain them.
- need human verification: Android app metadata now aligns `min_sdk_version` and `target_sdk_version` at API 30 to match the existing CI/release Android SDK platform install (`platforms;android-30` / `build-tools;30.0.3`), and the README Android validation note was updated accordingly. This still needs a real Android build/deploy check in case raising the minimum SDK reveals runtime/device-specific breakage.
- need human verification: `Code Viewer` now has a first real code-browsing pass instead of a placeholder surface: it page-browses like `Memory Viewer`, uses a bounded virtual-snapshot-backed x86/x64 decode window, renders breakpoint and branch gutters plus address/bytes/disassembly columns, and lets the user shift the current code window without trying to disassemble giant regions eagerly. The branch gutter was then tightened to a more conventional disassembler-style model: only branch edges get arcs, the gutter is wider, shortest visible spans get priority, and rendering is capped to a small number of lanes instead of trying to draw every visible control-flow edge.
- need human verification: Built-in ARM (`i_arm`, `i_arm64`) and PowerPC (`i_ppc32be`) instruction plugins are now default-enabled, use the instruction icon path, and scan end-to-end through the existing instruction-sequence scanner UX.
- need human verification: Rename collision handling in project explorer now blocks rename/move when destination already exists (no overwrite).
- need human verification: Project item ordering metadata now persists across add/create/delete/move/rename/reorder operations.
- need human verification: Adding scan results from TUI now targets selected project folder; if no folder is selected, items append at root.
- need human verification: Project hierarchy now supports F2 inline rename on the selected tree row with async refresh after submit, click-away cancel/select behavior, and project selector F2 inline rename support.
- need human verification: Project item rename now keeps the tree label synced with the item `name` property instead of diverging between visible label and backing filename.
- need human verification: Project hierarchy now supports double-click name-to-rename and double-click value-to-edit with a centered `DataValueBox` takeover, explicit commit/cancel actions, and close-project cleanup for address/pointer items.
- need human verification: Project hierarchy preview refresh now only requests visible/selected item previews, preserves cached preview text for off-screen rows, deduplicates duplicate address/pointer reads per refresh, and caps large fixed-array preview reads while still loading the full live value when entering value edit.
- need human verification: Project hierarchy array previews now collapse to element-style summaries like `[a, b, c, d, ...]` instead of dumping long full array strings into the tree row.
- need human verification: `VirtualSnapshots` now exist as a session-owned materialized memory-view layer in `squalr-engine-session`, and the GUI project hierarchy preview path now uses one named virtual snapshot instead of list-command-coupled preview refresh.
- need human verification: Added a dockable GUI `Memory Viewer` with results-style virtual-page navigation, sparse `??`-until-read hex/ASCII rendering, and visible-chunk refresh through a dedicated virtual snapshot.
- need human verification: Memory viewer refresh now preserves the selected page by base address instead of raw page index, prefers the first module-backed page on initial load, and labels pages as unreadable when visible chunk reads fail.
- need human verification: Project hierarchy context menus now support `Open in Memory Viewer` for address/pointer items, opening the viewer on the target address, resolving module-relative addresses, and scrolling the containing row into view.
- need human verification: Docked windows now support title-bar maximize, title-bar double-click maximize, and tab double-click maximize, transfer maximize state to the newly active sibling tab inside tab groups instead of blanking the dock, clip memory viewer row rendering to panel bounds, and visually mark maximized windows with a highlighted header plus `Fullscreen` badge.
- need human verification: Scan-result and memory-viewer add-to-project now use `module+0xOFFSET` names whenever module metadata exists instead of plain offsets.
- need human verification: Memory viewer now renders hex bytes in the theme hexadecimal color, clears drag-selection mode on mouse release, supports restored cross-row drag selection without stale follow-on drags, arrow-key cursor movement, merged per-row selection bands, a blinking hex nibble caret, forward byte-by-byte live hex writes until the end of the page, and add-to-project from the context menu.
- need human verification: Memory viewer now has a right-side interpretation panel with canonical common-add icon buttons per interpreted type, truncated array/hex previews, aligned numeric array interpretations, exact-size scalar interpretation gating, and `Ctrl+A` full-page selection; add-to-project still defaults to `u8` for one byte or `u8[n]` for multi-byte selections unless an interpretation row overrides the type.
- need human verification: Project-item memory-viewer opens now select the full runtime value byte span, project tree array previews are capped/truncated to short element summaries, and Struct Viewer array runtime values now open through an `Edit in Memory Viewer` button instead of inline `DataValueBox` editing.
- need human verification: TUI project workspace now exposes the Struct Viewer alongside the project explorer, the TUI project explorer skips list-owned preview reads and instead overlays visible/selected preview text from a shared virtual snapshot while preserving cached preview text across metadata refreshes, and pressing `o` on a selected address item opens the TUI memory viewer at that address.
- need human verification: TUI now has an `F6` Memory workspace with a readonly memory viewer that loads page metadata through `MemoryQueryRequest`, starts on the first module-backed page, renders unread bytes as `??`, and refreshes only visible chunk queries through its own virtual snapshot.

## Important Information
Append important discoveries. Compact regularly ( > ~40 lines, compact to 20 lines)

- Follow-up memory-viewer interpretation panel style pass: the interpretation side panel now paints with `theme.background_panel` and no border stroke instead of its own darker fill plus 1px outline, which should make it sit flush with the main memory-view background. Reverified with `cargo fmt --all`, `cargo check -p squalr`, and `cargo test -p squalr memory_viewer -- --nocapture`.
- Follow-up code-viewer header label pass: the branch-arrow gutter header now explicitly says `Jumps`, which reads more cleanly than an unlabeled spacer. Reverified with `cargo fmt --all` and `cargo check -p squalr`; the broader `cargo test -p squalr code_viewer -- --nocapture` path hit the same pre-existing Windows linker `LNK1201` `.pdb` write issue on this machine.
- Follow-up viewer polish pass: Code/Memory Viewer headers now use the same heavier foreground header treatment as scan results and add a thin horizontal separator below the header row, Code Viewer selection changes now cancel active inline edits through `CodeViewerViewData`, and the theme warning color was darkened to `#CC6633` / `#994D26`. The underfill prompt now says `code that does nothing`, while the tooltip still uses `no-operations`. Reverified with `cargo fmt --all`, `cargo check -p squalr`, `cargo test -p squalr code_viewer -- --nocapture`, `cargo test -p squalr memory_viewer -- --nocapture`, and `cargo test -p squalr views::code_viewer::view_data::code_viewer_view_data::tests::selecting_new_instruction_cancels_edit_in_progress -- --exact`.
- Follow-up viewer/scanner cleanup pass: removed the memory/code viewer empty-state warning labels, fixed format-compatibility gating so an empty data-type selection no longer disables unrelated types, and restyled the Code Viewer instruction size-mismatch actions with roomier warning rows plus real colored `Button` controls (`Fill + Write` blue, `Write Anyway` amber). Reverified with `cargo fmt --all`, `cargo check -p squalr`, `cargo test -p squalr code_viewer -- --nocapture`, `cargo test -p squalr memory_viewer -- --nocapture`, and `cargo test -p squalr ui::widgets::controls::data_type_selector::data_type_selector_view::tests::empty_selection_keeps_all_data_types_compatible -- --exact`.
- Follow-up viewer header pass: Code Viewer and Memory Viewer now reserve a thin content-header row under the toolbar and render their labels from the same live column geometry used by the row and separator layout, so the headers stay aligned when splitters move. Code Viewer labels the breakpoint gutter as `BP`, intentionally leaves the jump gutter unlabeled, and labels `Address`, `Bytes`, and `Assembly`; Memory Viewer labels `Address`, `Bytes`, and `String UTF-8` over the left content pane only. Reverified with `cargo fmt --all`, `cargo check -p squalr`, `cargo test -p squalr code_viewer -- --nocapture`, and `cargo test -p squalr memory_viewer -- --nocapture`.
- Follow-up viewer column-layout pass: Code Viewer and Memory Viewer now keep persistent splitter ratios in their view-data, paint scan-results-style vertical separators across the live row region, and separate fixed address columns from movable content columns instead of relying on hard-coded byte/text offsets. Code Viewer now uses a draggable `bytes <-> instruction` divider while keeping the breakpoint/branch/address side stable, and its inline editor/warning alignment follows the live text column. Memory Viewer now uses a draggable `hex <-> ascii` divider while keeping the address side stable, and hidden cells are clipped correctly so moving the divider intentionally occludes content instead of letting hex/ascii paint through each other. Reverified with `cargo fmt --all`, `cargo test -p squalr code_viewer -- --nocapture`, `cargo test -p squalr memory_viewer -- --nocapture`, and `cargo check -p squalr`.
- Follow-up viewer polish pass: Code Viewer now draws extra facade separators between the breakpoint gutter, jump gutter, and address column, both viewers add right-side breathing room before the address facade bar, the default Code Viewer bytes split is noticeably narrower, and the inline instruction editor now stays left-aligned with a 256px cap instead of stretching across the whole asm column. Reverified with `cargo fmt --all`, `cargo test -p squalr code_viewer -- --nocapture`, `cargo test -p squalr memory_viewer -- --nocapture`, and `cargo check -p squalr`.
- Follow-up edit-row height pass: Code Viewer now allocates a taller row height for the instruction currently being edited so the inline `DataValueBox` no longer has to live inside a normal `22px` disassembly row. Reverified with `cargo fmt --all` and `cargo check -p squalr`; the broader `cargo test -p squalr code_viewer -- --nocapture` path still hit the pre-existing Windows linker `LNK1201` `.pdb` write issue on this machine.
- Follow-up code-viewer editing pass: x86/x64 instruction-typed project items now route into `Code Viewer` instead of `Memory Viewer` from both project hierarchy and Struct Viewer, `InstructionSet` now exposes ISA-provided no-operation fill bytes for short rewrites, and Code Viewer now supports double-click inline instruction editing with exact-size commit, under-size `Fill + Write` using ISA NOPs, and over-size `Write Anyway` warnings. Reverified with `cargo fmt --all`, `cargo check -p squalr`, `cargo check -p squalr --tests`, `cargo test -p squalr-engine-api normalize_instruction_data_type_id_extracts_base_data_type_id`, `cargo test -p squalr-plugin-instructions-x86`, `cargo test -p squalr views::code_viewer::view_data::code_viewer_view_data::tests::evaluate_instruction_edit_commit_returns_exact_write_plan -- --exact`, `cargo test -p squalr views::code_viewer::view_data::code_viewer_view_data::tests::evaluate_instruction_edit_commit_sets_pending_fill_with_nops -- --exact`, `cargo test -p squalr views::code_viewer::view_data::code_viewer_view_data::tests::evaluate_instruction_edit_commit_sets_pending_overwrite -- --exact`, and `cargo test -p squalr views::project_explorer::project_hierarchy::project_hierarchy_view::tests::should_open_project_item_in_code_viewer_returns_true_for_instruction_data_type -- --exact`.
- Follow-up shared inline editor pass: `DataValueBoxView` now has an opt-in multiline mode for consumers that need it, while Code Viewer instruction editing reuses the shared control inline in a single-line configuration instead of a custom `TextEdit`. The edit state is now a real string-format `AnonymousValueString`, the inline row stays anchored to the selected address/span, and the Code Viewer context menu now exposes `Edit Instruction(s)` in addition to `Add Instruction(s) to Project`. Reverified with `cargo fmt --all`, `cargo check -p squalr`, `cargo test -p squalr views::code_viewer::view_data::code_viewer_view_data::tests::request_instruction_edit_uses_selected_instruction_span -- --exact`, `cargo test -p squalr views::code_viewer::view_data::code_viewer_view_data::tests::evaluate_instruction_edit_commit_returns_exact_write_plan -- --exact`, `cargo test -p squalr views::code_viewer::view_data::code_viewer_view_data::tests::evaluate_instruction_edit_commit_sets_pending_fill_with_nops -- --exact`, and `cargo test -p squalr code_viewer -- --nocapture`.
- Follow-up code-viewer UX rollback: Code Viewer instruction editing no longer uses the multiline inline editor mode. Multi-instruction selections still open through the same explicit edit path, but the editor is back to a single-line assembly field with `;`-joined text, leaving the multiline `DataValueBox` behavior in place for the scanner only. Reverified with `cargo fmt --all`, `cargo check -p squalr`, `cargo test -p squalr views::code_viewer::view_data::code_viewer_view_data::tests::request_instruction_edit_uses_selected_instruction_span -- --exact`, and `cargo test -p squalr code_viewer -- --nocapture`.
- Follow-up code-viewer in-row edit pass: Code Viewer no longer injects a separate pseudo-row for instruction editing. The edited row now keeps its address/byte columns and swaps only the instruction text column into the shared `DataValueBox`, so the edit state feels like it appears in place where the disassembly text was. Warning rows still appear underneath only when a size-mismatch decision is needed. Reverified with `cargo fmt --all`, `cargo check -p squalr`, and `cargo test -p squalr code_viewer -- --nocapture`.
- Follow-up scanner multiline pass: the element scanner now uses the shared multiline `DataValueBox` for `i_*` instruction types too, with taller constraint rows and hint text that points users at newline-separated assembly input instead of pretending instruction scans are scalar values. Reverified with `cargo fmt --all`, `cargo check -p squalr`, and `cargo test -p squalr views::element_scanner::scanner::view_data::element_scanner_view_data::tests::get_container_mode_label_returns_sequence_for_instruction_data_type -- --exact`.
- Follow-up scan-type compatibility pass: `DataTypeSelectorView` now has an opt-in format-compatibility gating mode, and the element scanner uses it so incompatible scan data types are faded/disabled instead of silently mixing families. Compatibility is currently defined by identical supported `AnonymousValueStringFormat` sets, which keeps the rule generic and plugin-friendly. Reverified with `cargo fmt --all`, `cargo check -p squalr`, `cargo test -p squalr ui::widgets::controls::data_type_selector::data_type_selector_view::tests::normalize_supported_formats_sorts_and_deduplicates -- --exact`, and `cargo test -p squalr ui::widgets::controls::data_type_selector::data_type_selector_view::tests::ordered_selectable_data_types_preserves_builtin_order_for_filtered_types -- --exact`.
- Follow-up viewer layout correction: the address-seek `DataValueBox` plus apply arrow now live in the header for both viewers, but the footer navigation bars were intentionally preserved. Code Viewer’s footer regained its viewport field, Memory Viewer’s footer returned to page navigation plus stats, and the address control now relies on preview text instead of an explicit `Go` label. Reverified with `cargo fmt --all`, `cargo test -p squalr code_viewer`, `cargo test -p squalr memory_viewer`, and `cargo check -p squalr`.
- Follow-up viewer UX pass: both viewer headers were flattened back down to a single top bar, the go-to fields now initialize in hexadecimal mode so the shared `u64` validation path stops normalizing them to decimal, both viewers paint address columns with `theme.hexadecimal_green`, and Code Viewer now has contiguous instruction selection plus a right-click add-to-project path that creates one instruction-typed address item spanning the selected byte range via `i_x86[n]` / `i_x64[n]`. Reverified with `cargo fmt --all`, `cargo check -p squalr`, and `cargo check -p squalr --tests`.
- Follow-up footer cleanup: Code Viewer’s footer no longer exposes the stray `View` viewport-start editor and now mirrors Memory Viewer’s footer pattern again, with page navigation on the top row and centered page/module stats on the bottom row. Reverified with `cargo fmt --all` and `cargo check -p squalr`.
- Follow-up header cleanup: Code Viewer no longer exposes the misleading viewport-shift header buttons; navigation is now just refresh, page nav in the footer, and explicit address seek in the header. Reverified with `cargo fmt --all` and `cargo check -p squalr`.
- The viewer-navigation UX is less jumpy now. Code Viewer row selection no longer schedules an automatic scroll-to-center just for clicking a local instruction, both viewers now treat `Esc` as “clear current selection,” and both footers now have a `Go` address `DataValueBox` plus explicit apply arrow. Address seeks now clamp to the nearest available page/address instead of failing silently when the requested address is outside the current page set. Reverified with `cargo fmt --all`, `cargo test -p squalr code_viewer`, `cargo test -p squalr memory_viewer`, and `cargo check -p squalr`.
- Added `ASM.md` audit: the fastest path to assembly scanning is an ISA-aware assemble-to-bytes frontend that reuses the existing byte-array / masked-pattern scan pipeline; for many-ISA support, instruction handling should become its own plugin capability with major ISAs shipped as built-in plugins, while host OS only influences default ranking instead of hard-selecting the backend.
- Android SDK metadata was split before this pass: the crate declared `min_sdk_version = 23` and `target_sdk_version = 30`, while CI/release automation was already provisioning `platforms;android-30` and `build-tools;30.0.3`. This pass aligned the crate metadata upward to API 30 and updated the README validation note to match. The only remaining Android-specific strings nearby are toolchain probe fallbacks like `aarch64-linux-android21-clang`, which are NDK executable names rather than app manifest SDK policy. Reverified with `cargo check -p squalr`.
- The separate `Code Viewer` window is now a real x86/x64 code browser instead of a placeholder. It reuses `MemoryQueryRequest` plus session-owned virtual snapshots for page metadata and byte materialization, but it deliberately decodes only a bounded viewport window with backtrack instead of attempting whole-page disassembly. The current view renders a breakpoint gutter, branch/jump connector gutter, address column, bytes column, and disassembly text, with toolbar/footer controls for refresh, page navigation, and shifting/jumping the decode window. The x86 plugin now also exposes structured disassembly rows with branch-target metadata so the GUI does not have to scrape formatted strings to draw code-flow affordances. Reverified with `cargo fmt --all`, `cargo test -p squalr-plugin-instructions-x86`, `cargo test -p squalr code_viewer`, and `cargo check -p squalr`.
- The first branch-gutter pass was too graph-like and chaotic, so it was tightened toward the normal disassembler pattern: call instructions no longer produce jump arcs, the branch gutter is wider, visible branches are lane-assigned by shortest span first, and rendering is capped to a few lanes so the margin stays readable instead of turning into spaghetti. Reverified with `cargo fmt --all`, `cargo test -p squalr-plugin-instructions-x86 x86_instruction_set_disassemble_block_does_not_report_call_targets_as_jump_lines`, and `cargo check -p squalr`.
- Follow-up library study recorded in `ASM.md`: the `asm` crate was a dead-end for Squalr, `asm-rs` is useful mainly for its lexer -> parser -> IR -> linker -> encoder pipeline shape, and `dynasm-rs` is useful mainly for its separation of shared runtime/relocation infrastructure from ISA-specific behavior; the recommended Squalr boundary is now a shared instruction toolkit in-engine with ISA plugins owning only register tables, operand rules, lowering, and backend integration such as `iced-x86`.
- Multi-ISA proof-out is now real, not just architectural: built-in plugin packages now include `builtin.instruction-set.arm-family` with `i_arm` / `i_arm64` and `builtin.instruction-set.powerpc-family` with `i_ppc32be`; `squalr-engine-api` now also exposes shared instruction-value helpers plus `#imm` parsing so non-x86 families can reuse the same string/hex data-type plumbing instead of reimplementing it. Current ARM subset covers `nop`, `bx`, `b/bl`, immediate `mov/add`, and simple `ldr/str`; current PowerPC subset covers `nop`, `blr`, `b/bl`, `li`, `addi`, `lwz`, `stw`, and `mr`. Reverified with `cargo fmt --all`, `cargo test -p squalr-engine-api instruction_set::instruction_sequence_parser::tests`, `cargo test -p squalr-plugin-instructions-arm`, `cargo test -p squalr-plugin-instructions-powerpc`, `cargo test -p squalr-plugin-builtins`, `cargo test -p squalr-engine-session registry_exposes_builtin_`, `cargo test -p squalr-engine element_scan_request_finds_`, and `cargo check -p squalr`.
- The dock layout now treats `Output` as the anchor for auxiliary/runtime-inspection windows: `Plugins`, `Memory Viewer`, and the new separate `Code Viewer` now default into that same tab group instead of being scattered across scan/settings surfaces. Reverified with `cargo test -p squalr default_layout_places_output_related_windows_in_same_tab_group` and `cargo check -p squalr`.
- Started executing that architecture for the x86/x64 plugin: `squalr-engine-api` now provides a shared instruction parsing toolkit (`parse_instruction_sequence`, parsed instruction/operand models, immediate parsing, memory-operand parsing with optional size hints, and normalized formatting), while the x86 plugin now focuses on x86-specific register/memory lowering and `iced-x86` encoding; verified with `cargo test -p squalr-engine-api instruction_set::instruction_sequence_parser::tests`, `cargo test -p squalr-plugin-instructions-x86`, `cargo test -p squalr-engine element_scan_request_finds_x86_instruction_sequence_matches`, and `cargo check -p squalr`.
- The x86/x64 plugin no longer relies on a hand-curated mnemonic switch for normal explicit-operand instructions: it now builds opcode candidates from `iced-x86` metadata, matches parsed operands against `OpCodeOperandKind`, and sets `Instruction` operands directly before encoding. This widened working coverage to general register instructions, shorthand `inc [addr]` in x86, SSE / AVX register forms, control-register moves, scaled-index memory expressions, segment overrides, memory broadcast suffix parsing, AVX-512-style decorators like `{k1}` / `{z}` / `{sae}` / `{rd-sae}`, and now branch labels with multi-pass short-vs-near selection (for example `start: inc eax; jne start` and forward jumps to far labels). The shared parser also accepts decorator-only trailing tokens in the canonical text that `iced-x86` sometimes emits (for example `..., {rd-sae}`), which keeps assemble -> disassemble -> reassemble stable for those forms. Current known frontend gaps are data directives, explicit prefix-driven syntax variants, richer expression/linking features beyond plain labels, and broader syntax sugar beyond explicit operands. Reverified with `cargo fmt --all`, `cargo test -p squalr-engine-api instruction_set::instruction_sequence_parser::tests`, `cargo test -p squalr-plugin-instructions-x86`, `cargo test -p squalr-engine element_scan_request_finds_x86_instruction_sequence_matches`, and `cargo check -p squalr`.
- Added a dedicated x86/x64 instruction parser corpus in `plugins/squalr-plugin-instructions-x86/src/instruction_parser_corpus_tests.rs`. It now covers representative operand-form categories across x86 and x64, including no-operand, unary register/memory, register-register, register-memory, memory-register, memory-immediate, fixed-register operands (`cl`), ternary GPR forms, segment/control/debug-register moves, MMX, SSE, AVX, AVX-512 `zmm`, mask registers, bound registers, AMX tile registers, and 4-/5-operand forms like `insertq` and `vpermil2ps`. The round-trip subset intentionally excludes x87 canonicalizations that disassemble to a different but equivalent textual form (`fadd st0, st1` -> `fadd st1`). Reverified with `cargo test -p squalr-plugin-instructions-x86`, `cargo test -p squalr-engine-api instruction_set::instruction_sequence_parser::tests`, `cargo test -p squalr-engine element_scan_request_finds_x86_instruction_sequence_matches`, and `cargo check -p squalr`.
- Implemented the first instruction slice: added `PluginCapability::InstructionSet`, a built-in `builtin.instruction-set.x86-family` plugin, and ISA-owned `i_x86` / `i_x64` data types that scan end-to-end through the existing byte-array path; the x86/x64 backend is now powered by pure-Rust `iced-x86` for encoding/decoding/formatting with a thin text frontend currently covering `mov reg, imm`, `push/pop reg`, `inc/dec reg`, `nop`, and `ret`; verified with `cargo test -p squalr-plugin-instructions-x86`, `cargo test -p squalr-plugin-builtins builtins_include_x86_family_instruction_plugin_package`, `cargo test -p squalr-engine-session registry_exposes_builtin_x86_instruction_plugin_capabilities`, `cargo test -p squalr-engine element_scan_request_finds_x86_instruction_sequence_matches`, and `cargo check -p squalr`.
- The x86-family instruction data types now live in concrete files `data_type_i_x86.rs` and `data_type_i_x64.rs`, with shared string/byte conversion helpers isolated in `x86_family_instruction_value.rs`; the old generic `instruction_data_type.rs` module was removed to keep filenames aligned with their actual contents.
- Project plugin persistence now stores enablement overrides relative to plugin defaults instead of a full enabled-plugin snapshot, so newly introduced default-enabled built-ins like the x86 instruction plugin stay on for existing projects unless explicitly overridden; project loading still accepts the legacy `plugins: [...]` list and upgrades it into enabled overrides on save, and the scanner/type selector now render `i_x86` / `i_x64` in short form instead of `Unknown`.
- `DataValueBoxView` now normalizes unsupported display formats back to a supported/default format for the selected data type, the scanner/results views proactively snap instruction inputs/editors away from stale decimal mode, and scan-result rows now fall back to scan-time display strings when live rereads cannot satisfy the requested format so `i_x86`/`i_x64` results are less likely to degrade into `??`.
- The scanner now treats `i_*` data types as instruction sequences rather than generic arrays: the container-mode control is locked to `Sequence`, multi-instruction scans are expected via `;` or newline separators, and instruction input no longer routes through the generic comma-delimited array UX.
- Added shared sort-order reconciler in `squalr-engine` project-item command executors to prevent manifest replacement and keep order entries complete.
- TUI hierarchy graph previously sorted by filesystem path; now consults project manifest sort order when available, then falls back to path order.
- Verified with `cargo check -p squalr-engine -p squalr-tui` and `cargo test -p squalr-tests --test project_items_command_tests`.
- Project hierarchy rename now refreshes only after the async rename response, preserves selection/expanded state across the renamed path, and cancels cleanly when another row is selected.
- Project selector now enters inline rename from F2 on the selected project and clears rename state when selection changes or rename is canceled.
- Tree rows render the project item `name` property when present; the rename executor now updates that stored name alongside the path rename so labels no longer lag behind filenames.
- Project hierarchy row double-clicks are now split by hitbox: the name region enters rename, while the trailing value region opens a takeover editor backed by the existing runtime-value write path.
- Project hierarchy refresh now drops stale rename/value/delete takeovers when the targeted project items disappear, preventing orphaned modals after closing or switching projects.
- `ProjectItemsListRequest` now accepts an internal-only optional `preview_project_item_paths` filter; the GUI and TUI can now pass `Some(Vec::new())` to skip list-owned preview reads entirely and overlay preview text from session-owned virtual snapshots instead.
- GUI project hierarchy preview refresh now caches duplicate `(address, module, layout)` reads and duplicate pointer-chain evaluations within a refresh pass, and large fixed arrays are preview-read with a capped anonymous layout instead of the full array.
- Project-item preview formatting now prefers element-aware array truncation over raw character slicing, so both truncated reads and long full-array display strings collapse to short summaries like `[0, 1, 2, 3, ...]`.
- Added a first-pass `VirtualSnapshots` subsystem to `squalr-engine-session`; queries are declarative replacement sets, refreshes are async per snapshot, and results carry materialized address/pointer reads plus pointer-path metadata.
- GUI project hierarchy now keeps project item metadata refresh on `ProjectItemsList`, but it skips list-driven previews and instead builds consumer-specific virtual snapshot queries for visible/selected project items, then overlays preview fields from the latest snapshot generation.
- Entering project-item value edit now performs a dedicated full live memory read for the item's configured type, so preview truncation does not leak into editing; the current takeover view remains the seam for a future dedicated byte/hex editor.
- Added `MemoryQueryRequest/Response` so GUI consumers can query routed virtual memory pages and modules for the currently opened process through the existing memory-view plugin path.
- The new GUI memory viewer keeps page metadata separate from byte materialization: page lists come from `MemoryQueryRequest`, while visible hex rows issue aligned `u8[N]` virtual-snapshot chunk queries and cache materialized bytes sparsely per page.
- The memory viewer currently enumerates raw usermode committed pages, so genuinely unreadable/guard pages can still exist; the viewer now preserves selection by page base address across refresh and explicitly marks pages as unreadable when visible chunk reads fail.
- Memory viewer page stats now show the module name directly with a `(No Module)` fallback instead of prefixing labels with `Module`.
- Memory viewer initial load currently resolves to the first page containing the first reported module base address, which can yield a nonzero page index if the raw page list begins with unowned gaps.
- Memory viewer focus requests are now asynchronous and module-aware: the viewer stores a pending target, resolves module-relative offsets against its current module list after refresh, then applies a one-shot row scroll on the containing page.
- Memory viewer focus requests can now carry a byte span, so opening a project item or Struct Viewer array value in Memory Viewer jumps to the containing page, scrolls to the address, and preselects the requested runtime byte range.
- Dock maximize is transient UI state in `DockRootViewData` rather than persisted layout state; the dock root swaps normal split rendering for the maximized panel, a footer-tab double click now toggles maximize for that sibling, and when a maximized window belongs to a tab group the maximize state now follows the currently active sibling tab.
- Maximized dock windows now expose a lightweight `is_window_maximized` helper in `DockRootViewData`, and the title bar uses that to switch to a highlighted header state, render a `Fullscreen` badge, and support drag-area double-click toggling in addition to the existing maximize button/tab gesture.
- `DockedWindowView` now clips child content to the dock content rect, and the memory viewer additionally clips each painted hex row to the visible row rect to stop overflow.
- `ProjectItemsCreateRequest` address items can now optionally carry explicit `address` and `module_name` fields, and the create executor seeds those into new address project items instead of always creating zero-address placeholders.
- Project-item names created from scan results or memory-viewer addresses now force module-relative `module+0xOFFSET` text when module metadata exists, independent of the current scan-result display mode.
- Memory viewer selection is contiguous-address based within the current page; drag selection now follows the live pointer only while an explicit drag gesture is active, mouse release clears the drag-selection state, arrow keys move or extend the active cursor by byte/row, and live hex editing uses a cursor/nibble state that writes one byte at a time and keeps advancing until the current page ends.
- Memory viewer now splits its content area into the main hex grid plus a right-side interpretation panel; the panel shows truncated raw hex, UTF-8, scalar interpretations, and aligned array interpretations (`u8/i8/u16/i16/u32/i32/f32/u64/i64/f64`) when the current selection size lines up.
- Scalar interpretation rows in the memory viewer now only appear when the selection byte count exactly matches that scalar type size; larger selections only expose array interpretations that consume the entire selection.
- Memory viewer interpretation rows can now create project items as the exact displayed type by routing through an explicit `data_type_id` override; if no interpretation override is used, selection-aware add still creates `u8` for one byte or `u8[n]` for multi-byte selections.
- Memory viewer interpretation actions now use the shared `icon_handle_common_add` icon-button pattern instead of text labels, so the row label/tooltip carries the type context while matching the rest of the GUI.
- Memory viewer keyboard navigation now supports `Ctrl+A` / command-select-all for the entire current page, anchoring the selection to the full page while leaving hex editing focused at the first byte.
- Struct Viewer now routes array-typed runtime value editing to the Memory Viewer instead of inline value-box editing, and project/tree preview reads for fixed arrays are capped down to the same short preview scale rather than reading large prefixes just to render a summary.
- `MemoryResponse::Query` now has a CLI response handler, so adding memory-page queries no longer breaks `squalr-cli` exhaustive response matching in CI.
- TUI Project workspace now renders `Project Explorer -> Struct Viewer -> Output` when a project is open instead of hiding the already-synced struct viewer state behind an unreachable pane enum variant.
- TUI project previews now request project-item metadata without preview reads, preserve cached address/pointer preview strings inside `ProjectExplorerPaneState`, and materialize visible/selected preview values from `tui_project_item_previews` virtual snapshot queries.
- TUI Memory Viewer is currently readonly and address-item-focused: it enumerates pages/modules via `MemoryQueryRequest`, uses selection-relative row viewporting to issue `u8[N]` chunk queries against `tui_memory_viewer`, and `o` from the project tree currently focuses address items but not pointer targets.
- `squalr-tests/tests/project_items_command_tests.rs` now initializes `ProjectItemsListRequest.preview_project_item_paths` and `ProjectItemsCreateRequest.address/module_name`, keeping CI aligned with the newer project-item request shapes; reverified with `cargo test -p squalr-tests --test project_items_command_tests`, `cargo check -p squalr-cli`, `cargo test -p squalr-tui`, and `cargo check -p squalr`.
