# Agentic Current Task
Our current task, from `README.md`, is:
`pr/todo`

# Notes from Owner
- Assume any unstaged/uncommitted file changes are from a previous iteration, or from the human author giving guidance. Keep them if they look good; do not ask about them by default.
- Assume any connected Android devices are rooted, and assume macOS has SIP disabled.
- Do not declare behavior as fixed. Use "needs human verification" after implementation and validation.
- Alpha-stage data compatibility is not required for this refactor. Prefer a clean model over preserving old address/pointer/symbol-ref project item properties.

## Current Tasklist

- Continue public API/docs cleanup after adding the README usage overview.

## Important Information

- Current target-abstraction refactor pass split target provider traits and shared process-query types into `squalr-engine-targets`, renamed the native platform backend crate to `squalr-engine-targets-native`, and updated the Dolphin memory-view plugin/tests/session routing to depend on the new target/native crate boundary. `squalr-engine-session` still owns memory-view routing around those providers. Validated with `cargo fmt --all`, `cargo check -p squalr --locked`, `cargo check -p squalr-cli --locked`, `cargo check -p squalr-tui --locked`, `cargo test -p squalr-engine-targets --locked`, `cargo test -p squalr-engine-targets-native --locked`, `cargo test -p squalr-engine-session --locked`, `cargo test -p squalr-plugin-memory-view-dolphin --locked`, `cargo test -p squalr-tests --test os_behavior_command_tests --locked`, `cargo test -p squalr-tests --test process_command_tests --locked`, `cargo test -p squalr-tests --test memory_command_tests --locked`, `cargo test -p squalr-engine process --lib --locked`, and legacy-native-crate-name search. Needs human verification for native process open/list, memory read/write/query, and Dolphin memory-view target routing.
- Current API-boundary cleanup merged `squalr-engine-domain` into `squalr-engine-api`, removed the domain crate from the workspace, moved the former domain model modules under API, and kept API-only helpers such as data value preview formatting. Validated with `cargo fmt --all`, `cargo check -p squalr-engine-api --offline`, `cargo check -p squalr --locked`, `cargo check -p squalr-cli --locked`, `cargo check -p squalr-tui --locked`, `cargo test -p squalr-engine-api --locked`, `cargo test -p squalr-engine-scanning --locked`, `cargo test -p squalr-engine-session --locked`, and legacy-domain-crate-name search. Needs human verification for downstream library consumers that previously depended directly on `squalr-engine-domain`.
- Current scanning boundary cleanup removed process I/O from `squalr-engine-scanning`: deleted the legacy `ElementScanExecutor`, `ScanExecutionContext`, `ValueCollector`, and snapshot-region memory reader modules; pointer scan execution/validation now operate on precollected snapshots; `squalr-engine` owns the process-backed `SnapshotValueCollector` adapter and app command executors now collect values before calling `ElementScanner` / `PointerScanExecutor` / `PointerScanValidator`. The interleaved memory read mode was removed from API settings, TUI cycling/labels, and settings command tests. The scanning manifest now avoids target/session/native dependencies and unused direct dependencies. Validated with boundary searches for process/native/session symbols in `squalr-engine-scanning`, `cargo fmt --all`, `cargo test -p squalr-engine-scanning --locked`, `cargo test -p squalr-engine-api --locked`, `cargo test -p squalr-engine scan --lib --locked`, `cargo test -p squalr-tests --test settings_command_tests --locked`, `cargo test -p squalr-engine-session --locked`, `cargo check -p squalr --locked`, `cargo check -p squalr-cli --locked`, `cargo check -p squalr-tui --locked`, and `git diff --check`. Needs human verification for GUI/CLI/TUI element scans, pointer scan start/validate, explicit value collection, and settings UX after dropping interleaved mode.
- Current settings boundary cleanup moved persisted scan settings out of `squalr-engine-scanning` and into `squalr-engine-session::settings::scan_settings_store::ScanSettingsStore`; `squalr-engine-session` no longer depends on `squalr-engine-scanning`, and `squalr-engine-scanning` no longer depends on `serde_json` or exports app settings. The store now uses `OnceLock` instead of the previous unsafe singleton and logs read/parse/write failures. Engine command executors and the session freeze task now consume the session settings store. Validated with stale settings path searches, `cargo fmt --all`, `cargo test -p squalr-engine-scanning --locked`, `cargo test -p squalr-engine-session --locked`, `cargo test -p squalr-engine scan --lib --locked`, `cargo test -p squalr-tests --test settings_command_tests --locked`, `cargo check -p squalr --locked`, `cargo check -p squalr-cli --locked`, `cargo check -p squalr-tui --locked`, and cargo-tree checks for the session/scanning dependency split. Needs human verification for persisted scan settings loading/saving across GUI/TUI/CLI session mode.
- Current project-manager boundary cleanup moved the concrete `ProjectManager` and its `notify` watcher dependency from `squalr-engine-api` into `squalr-engine-session::projects`; API now keeps only project data models plus a small `ProjectContext` trait exposed through `EngineExecutionContext::get_project_manager`. `notify` is no longer a normal dependency of `squalr-engine-api`; the watcher was preserved in session for future full project-directory behavior. Validated with concrete-manager/API dependency searches, `cargo fmt --all`, `cargo check -p squalr-engine-api --locked`, `cargo check -p squalr-engine-session --locked`, `cargo check -p squalr-engine --locked`, `cargo check -p squalr --locked`, `cargo check -p squalr-cli --locked`, `cargo check -p squalr-tui --locked`, `cargo test -p squalr-engine-api --locked`, `cargo test -p squalr-engine-session --locked`, `cargo test -p squalr-engine project --lib --locked`, `git diff --check`, and cargo-tree checks for the API/session dependency placement. Needs human verification for project open/close/save/export, project explorer refresh, project item mutations, and future watcher-backed project catalog behavior.
- Current project-refresh cleanup introduced a session-owned `projects::project_refresh` service with switches for internal project events and file-system watching; internal events are enabled, and file-system watching is controlled by the persisted scan setting below. Project item changes emit lightweight `ProjectItemsChangedEvent` payloads, project create/delete emits lightweight `ProjectCatalogChangedEvent`, the project explorer listens at the top level, and the hierarchy no longer performs a per-frame full-project signature diff. The watcher paths are scoped so catalog-root changes invalidate the project selector while opened-project changes invalidate the hierarchy. Validated with stale polling/watcher searches, `cargo fmt --all`, `cargo check -p squalr-engine-api --locked`, `cargo check -p squalr-engine-session --locked`, `cargo check -p squalr-engine --locked`, `cargo check -p squalr --locked`, `cargo check -p squalr-cli --locked`, `cargo check -p squalr-tui --locked`, `cargo test -p squalr-engine-api --locked`, `cargo test -p squalr-engine-session --locked`, `cargo test -p squalr-engine project --lib --locked`, and `git diff --check`. Needs human verification for GUI project create/delete/rename/open/close, project item add/delete/rename/move/reorder/update, and ensuring large project explorer refresh behavior feels responsive without the old polling diff.
- Current project-file-watcher settings cleanup makes file-system project refresh default-on and persistently disableable through `ScanSettings::project_file_system_watch_enabled`, with serde defaulting old scan settings to enabled. The project manager starts the project catalog watcher from `ProjectSettingsConfig`, open/close/rename update the opened-project watcher, watcher events are debounced, the project catalog root is created before watching, GUI/TUI settings toggle the runtime project manager immediately, and CLI/settings command parsing persists the new setting. The TUI memory workspace pane-order test was also corrected to include the existing memory interpretation pane. Validated with `cargo fmt --all`, `cargo test -p squalr-tui --locked`, `cargo test -p squalr-tests --test settings_command_tests --locked`, `cargo test -p squalr-engine-session --locked`, `cargo test -p squalr-engine-api --locked`, `cargo test -p squalr-engine project --lib --locked`, `cargo check -p squalr --locked`, `cargo check -p squalr-cli --locked`, `cargo check -p squalr-tui --locked`, `cargo tree -p squalr-engine-api --edges normal --depth 1`, and `git diff --check`. Needs human verification for GUI/TUI/CLI settings persistence, default-on watcher startup, project catalog refresh on external create/delete/rename, opened-project hierarchy refresh on external file changes, and refresh behavior on large projects.
- Current external-project-edit reconciliation cleanup makes deserialized projects/items clean in memory, clears project-item dirty state after successful item serialization, adds project-level clean/dirty helpers, and reconciles opened-project watcher events by touched path instead of reloading the whole opened project. Debounced file-system events now retain their paths: project item JSON creates/modifies load only the touched item file while preserving runtime activation for that item, deletes remove the matching item/subtree from memory, directory creates walk only the new subtree, rename-both removes the old path and upserts the new path, and `project.json` updates replace only `ProjectInfo`. Dirty in-memory projects should now mainly mean a mutation is in progress, a save failed, or a future non-proactive-save flow exists; those are not overwritten. Promotion now marks replacement project items dirty so proactive save writes their symbolic target updates. Validated with `cargo fmt --all`, `cargo test -p squalr-engine-api --locked`, `cargo test -p squalr-engine-projects --locked`, `cargo test -p squalr-engine-session project_refresh --locked`, `cargo test -p squalr-engine-session --locked`, `cargo test -p squalr-engine project --lib --locked`, `cargo check -p squalr --locked`, `cargo check -p squalr-cli --locked`, `cargo check -p squalr-tui --locked`, and `git diff --check`. Needs human verification for real watcher event shapes on Windows/macOS/Linux, editor atomic-save patterns, project catalog refresh on external create/delete/rename, opened-project hierarchy refresh on external file changes, symbol/project-info external edits, failed-save external edit behavior, and large project responsiveness.
- README usage docs now cover the supported app entrypoints, platform/front-end availability, plugin extension path, and library-consumer layers for byte scanners, custom target integrators, native process scanners, Squalr frontends/automation shells, and project/symbol tooling. Validated with `git diff --check`.
