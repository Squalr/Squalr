# Agentic Current Task
Our current task, from `README.md`, is:
`pr/symbol-authoring-2`

# Notes from Owner
- Assume any unstaged/uncommitted file changes are from a previous iteration, or from the human author giving guidance. Keep them if they look good; do not ask about them by default.
- Assume any connected Android devices are rooted, and assume macOS has SIP disabled.
- Do not declare behavior as fixed. Use "needs human verification" after implementation and validation.
- Alpha-stage data compatibility is not required for this refactor. Prefer a clean model over preserving old address/pointer/symbol-ref project item properties.

## Current Tasklist

- [X] Add resolver descriptors to the project symbol catalog as a new reusable symbol-authoring store.
- [X] Keep the initial resolver execution scope local to the current struct instance: local scalar fields plus type sizes.
- [X] Build a dedicated Symbol Resolvers window that edits resolver descriptors as a project-explorer-like tree, not as expression text. Needs human verification for live GUI ergonomics.
- [X] In the Struct Editor, let dynamic array counts and expression offsets pick a resolver from the global resolver list. Needs human verification for live GUI ergonomics.
- [X] Migrate PE header formulas to built-in/project resolver descriptors so `PE Headers` no longer depends on hardcoded inline expression strings.
- [X] Run focused domain/API/GUI tests for resolver serialization, evaluation, struct editor integration, Symbol Explorer resolution, and PE population.
- [X] Replace inline symbolic-expression authoring in the Symbol Struct Editor with a first-class resolver model completely. Needs human verification for live GUI ergonomics.
- [ ] Add local resolver dependency validation and cycle diagnostics at the resolver-descriptor/catalog level.
- [X] Remove the current expression text builder UI from Symbol Struct Editor field layout editing.
- [X] Route Symbol Struct Editor field details through Details Viewer and keep the opened-layout takeover to field entries plus row ordering/delete/insert controls. Needs human verification for live GUI ergonomics.
- [ ] Add global symbol-chain resolver inputs after the local resolver model stabilizes.

## Important Information

- The current symbolic-expression system is real but too low-level for users. It stores inline AST expressions on `SymbolicFieldDefinition` for dynamic array counts and explicit offsets, with text only at parse/display/serde boundaries.
- Added `SymbolicResolverDefinition` / `SymbolicResolverDescriptor`. Resolver nodes are currently literal, local field, type size, and binary arithmetic operation. The current `local field` node is really a relative field reference against the struct instance evaluating the resolver, and should be renamed/refined as `relative field`. Binary operations are intentionally fixed to two children for simpler editing and operator swapping.
- `ProjectSymbolCatalog` now stores reusable symbolic resolver descriptors.
- `SymbolicFieldCountResolution` and `SymbolicFieldOffsetResolution` now support `Resolver(String)` in addition to inline `Expression(...)`. The string boundary format is `resolver(id)`.
- The current resolver evaluator is intentionally narrow. It is robust enough for local struct-instance formulas over scalar fields and type sizes, but not for global project symbol-chain inputs, pointer dereference inside expressions, or conditional type/layout selection.
- The current runtime resolver evaluates formulas/resolvers against scalar fields collected from the same struct instance. Symbol Explorer feeds those scalar values through a dedicated virtual snapshot lane so formulaic layouts can converge over frames.
- The current cycle detection only rejects local field-name dependency cycles inside a struct draft. It is not a project-wide dependency graph.
- Added a Symbol Resolvers dock window. It is titled `Symbol Resolvers`, defaults into the same tab group as the scanner/Symbol Explorer area, and uses the themed project controls instead of raw egui buttons/combos/text edits. Needs human verification for live GUI ergonomics.
- The Symbol Resolvers window now has distinct Project Explorer-style surfaces. List mode is a flat resolver list; the top bar `+` enters create-name mode, a row edit button enters rename-name mode, and double-clicking a row opens the resolver tree. Selecting a resolver does not implicitly begin editing. Needs human verification for live GUI ergonomics.
- Resolver create mode only asks for a name and creates the default rooted expression after save. Resolver rename mode only edits the name and has the Project Editor-style cancel/save/delete surface for existing resolvers. Needs human verification for live GUI ergonomics.
- Resolver opened mode shows the rooted expression tree. There is no tree add menu or morph button; node kind/operator/type details route through the existing Details Viewer (`StructViewerViewData`). Changing a node to an operation creates the fixed two child slots, and changing it back removes nesting through the same Details Viewer combo path. Changing node kind refocuses Details Viewer because the selected node's editable fields change shape. Needs human verification for live GUI ergonomics.
- Resolver deletion now enters a delete-confirmation takeover from the Delete key or delete icon, with Enter confirming and Escape/cancel backing out. Needs human verification for live GUI ergonomics.
- Symbol Resolvers list rows now preview resolver usage counts, and resolver delete confirmation warns when existing dynamic count/offset references will become unresolved. Needs human verification for live GUI ergonomics.
- Resolver list rows draw the edit button at the far right edge. Literal resolver node details use a numeric `i64` value field with bin/dec/hex display formats and hex as the default Details Viewer format. Needs human verification for live GUI ergonomics.
- The Struct Editor now offers resolver pickers for dynamic array counts and explicit offsets instead of inline expression authoring.
- The Struct Editor field draft model now stores dynamic count and offset resolver ids, not inline expression text. Dynamic count editing always uses a resolver, and offset editing exposes only `Sequential` or `Resolver`. Existing expression-backed descriptors opened in the editor are demoted to empty resolver selections that require a resolver before saving. Needs human verification for live GUI ergonomics.
- Details Viewer focus updates now bump a revision counter. Main Window polls that revision and requests warning tab attention for Details Viewer when hidden, reusing the existing dock tab flash behavior. Needs human verification for live GUI ergonomics.
- Symbol Struct Editor delete confirmation now accepts Enter to confirm deletion when the window can handle shortcuts and the selected layout is deletable. Delete/trash icon affordances in the GUI audit now use `background_control_danger` / `background_control_danger_dark` backdrops, including toolbar/context-menu variants. Needs human verification for live GUI ergonomics.
- Selectable GUI lists now share ArrowUp/ArrowDown navigation behavior across Project Explorer, Element Scanner results, Pointer Scanner results, Symbol Explorer, Symbol Resolvers, Symbol Struct Editor, and Details Viewer. Project Explorer and Element Scanner results use Shift+Arrow to extend the range selection and plain Arrow to move the active selection while clearing the range end. Natural Delete-key deletion was added for Element Scanner results and the Symbol Resolvers list where the existing list action already supports deletion. Needs human verification for live GUI ergonomics.
- Element Scanner results now consume Up/Down at the scan-results window level before child controls render, and result row clicks no longer request egui keyboard focus. This keeps arrow navigation on entries instead of moving through freeze checkboxes or header controls. Needs human verification for live GUI ergonomics.
- Project Explorer and Symbol Struct Editor no longer show top-toolbar delete buttons; deletion remains reachable through natural keyboard/context/takeover flows. Needs human verification for live GUI ergonomics.
- Symbol Struct Editor deletion now permits deleting layouts with existing references. Catalog fix-up eagerly retargets symbol claims, module fields, and nested struct fields that used the deleted layout to raw `u8`, preventing dangling ids from binding to future structs. Needs human verification for live GUI ergonomics.
- Symbol Struct Editor field rows no longer render the redundant shape/offset summary text like `array | sequential` or `pointer (u64) | sequential`; the same state remains available in the actual field layout controls. Needs human verification for live GUI ergonomics.
- Project Explorer opened-project toolbar now keeps add item on the left and right-aligns close project to reduce accidental adjacency with common actions. Needs human verification for live GUI ergonomics.
- Struct Viewer pointer offset editor takeover now keeps the nav bar title-only and shows standard body actions: danger `Cancel` plus primary `Accept`. Needs human verification for live GUI ergonomics.
- Symbol Struct Editor and Symbol Resolvers takeovers now keep save/cancel/delete confirmation actions out of the nav bar and use body `Cancel`/`Accept` buttons. Symbol Explorer's Symbol Tree header now only shows left-aligned Add Module; rename/delete/open actions remain available through non-header flows. Needs human verification for live GUI ergonomics.
- Element Scanner results Delete-key handling now checks focus against the docked Element Scanner window and routes through the same `DeleteSelection` frame action used by selection deletion. Needs human verification for live GUI ergonomics.
- Element Scanner result deletion now optimistically removes selected visible refs from the GUI page, returns delete counts from the engine, warns on partial/no-op deletes, and compacts manual-deletion tombstones into the scan filters before the next element scan so stale global indices do not drift across rescans. Needs human verification for live delete-rampage behavior.
- Symbol Struct Editor, Symbol Resolvers, and Struct Viewer takeover panels now keep body action buttons at fixed 120x28 sizing and leave 8px bottom padding. Symbol Struct Editor resolver pickers now use the themed `ComboBoxView` instead of native egui combo boxes. Needs human verification for live GUI ergonomics.
- Symbol Struct Editor now separates selection, rename, and opened-layout editing. Selecting a struct layout focuses its name in Details Viewer, row edit opens rename-name mode, and opening a layout shows only field entries with row-local move/delete/insert-after controls. Selecting a field focuses all field shape/type/resolver details in Details Viewer through themed combo/value editors. Needs human verification for live GUI ergonomics.
- Symbol Struct Editor opened-layout field rows now use list-style row highlighting with painted text so preview text does not take selection. Field row controls are right-aligned, preview text sits left of the controls, and the row-local ordering/delete/insert icons use flat transparent button backgrounds. Needs human verification for live GUI ergonomics.
- The Resolver Editor should eventually own resolver construction, validation, dependency visualization, and preview diagnostics.
- Global resolver inputs are still a required next capability for broader use cases. They should be modeled as a structured symbol chain, not a free-text path. It should work like the existing ProjectItemAddress symbolic offset chain UX/model, except every chain entry must be a valid symbol segment and numeric bin/dec/hex pointer offsets are invalid. Pointer dereferences, module-base references, and conditional layout/type selection should be deferred until the resolver model is stable.
- Built-in PE population is now backed by PE resolver descriptors for dynamic offsets/counts. PE32 versus PE32+ selection is still plugin analysis today; resolver-based conditional layout selection is a later feature.
- Validation passed on 2026-05-05: `cargo test -p squalr-engine-domain --lib`, `cargo test -p squalr-engine-api --lib`, `cargo test -p squalr-plugin-symbols-pe`, and `cargo test -p squalr --lib`.
- Validation passed on 2026-05-06 after the Symbol Resolvers UI rewrite: `cargo test -p squalr --lib symbol_resolver_editor`, `cargo test -p squalr --lib dockable_window_settings`, and `cargo test -p squalr --lib`.
- Validation passed on 2026-05-06 after the tree/details correction: `cargo test -p squalr --lib symbol_resolver_editor` and `cargo test -p squalr --lib`.
- Validation passed on 2026-05-06 after simplifying Symbol Resolvers back to toolbar + tree only: `cargo test -p squalr --lib symbol_resolver_editor` and `cargo test -p squalr --lib`.
- Validation passed on 2026-05-06 after changing Symbol Resolvers to a single add menu and details node-type combo: `cargo test -p squalr --lib symbol_resolver_editor` and `cargo test -p squalr --lib`.
- Validation passed on 2026-05-06 after routing Symbol Resolver edits through Details Viewer instead of an in-window details strip: `cargo test -p squalr --lib symbol_resolver_editor` and `cargo test -p squalr --lib`.
- Validation passed on 2026-05-06 after refreshing Details Viewer when resolver node kind changes: `cargo test -p squalr --lib symbol_resolver_editor` and `cargo test -p squalr --lib`.
- Validation passed on 2026-05-06 after changing Symbol Resolvers to list mode plus explicit create/edit takeover: `cargo test -p squalr --lib symbol_resolver_editor` and `cargo test -p squalr --lib`.
- Validation passed on 2026-05-06 after splitting Symbol Resolvers into list, create-name, rename-name, and opened-tree surfaces: `cargo test -p squalr --lib symbol_resolver_editor` and `cargo test -p squalr --lib`.
- Validation passed on 2026-05-06 after right-aligning resolver row edit buttons and making literal details numeric: `cargo test -p squalr --lib symbol_resolver_editor` and `cargo test -p squalr --lib`.
- Validation passed on 2026-05-06 after splitting Symbol Struct Editor resolver fields from inline expression text: `cargo test -p squalr --lib symbol_struct_editor`, `cargo test -p squalr --lib symbol_resolver_editor`, and `cargo test -p squalr --lib`.
- Validation passed on 2026-05-06 after adding Details Viewer tab attention on focus updates: `cargo test -p squalr --lib struct_viewer` and `cargo test -p squalr --lib`.
- Validation passed on 2026-05-07 after adding Symbol Struct Editor Enter-to-confirm delete and danger delete icon backdrops: `cargo fmt --check`, `cargo test -p squalr --lib symbol_struct_editor`, `cargo test -p squalr --lib toolbar_menu`, and `cargo test -p squalr --lib`.
- Validation passed on 2026-05-07 after adding shared list keyboard navigation and natural Delete-key handling for additional lists: `cargo fmt --check`, `cargo test -p squalr --lib list_navigation`, and `cargo test -p squalr --lib`.
- Validation passed on 2026-05-07 after moving the Symbol Resolvers default dock placement to the scanner/Symbol Explorer area: `cargo fmt --check`, `cargo test -p squalr --lib dockable_window_settings`, and `cargo test -p squalr --lib`.
- Validation passed on 2026-05-07 after tightening Element Scanner result Up/Down navigation to entry selection only: `cargo fmt --check`, `cargo test -p squalr --lib element_scanner_results_view`, and `cargo test -p squalr --lib`.
- Validation passed on 2026-05-07 after adding Symbol Resolver delete confirmation and removing top-toolbar delete buttons from Project Explorer and Symbol Struct Editor: `cargo fmt --check`, `cargo test -p squalr --lib symbol_resolver_editor`, `cargo test -p squalr --lib symbol_struct_editor`, and `cargo test -p squalr --lib`.
- Validation passed on 2026-05-07 after allowing in-use Symbol Struct Editor deletes with `u8` catalog retargeting: `cargo fmt --check`, `cargo test -p squalr --lib symbol_struct_editor`, and `cargo test -p squalr --lib`.
- Validation passed on 2026-05-07 after changing the in-use Symbol Struct Editor delete confirmation reference fix-up text to the theme warning color: `cargo fmt --check` and `cargo test -p squalr --lib symbol_struct_editor`.
- Validation passed on 2026-05-07 after removing redundant Symbol Struct Editor field layout summary text: `cargo fmt --check` and `cargo test -p squalr --lib symbol_struct_editor`.
- Validation passed on 2026-05-08 after adding Symbol Resolver usage previews and delete warnings: `cargo fmt --check`, `cargo test -p squalr --lib symbol_resolver_editor`, and `cargo test -p squalr --lib`.
- Validation passed on 2026-05-08 after right-aligning Project Explorer close project: `cargo fmt --check` and `cargo test -p squalr --lib`.
- Validation passed on 2026-05-08 after moving Struct Viewer pointer offset editor actions out of the nav bar: `cargo fmt --check`, `cargo test -p squalr --lib struct_viewer`, and `cargo test -p squalr --lib`.
- Validation passed on 2026-05-08 after moving Symbol Struct Editor/Symbol Resolvers takeover actions out of nav bars and simplifying the Symbol Tree header: `cargo fmt --check`, `cargo test -p squalr --lib symbol_struct_editor`, `cargo test -p squalr --lib symbol_resolver_editor`, `cargo test -p squalr --lib symbol_explorer`, and `cargo test -p squalr --lib`.
- Validation passed on 2026-05-08 after routing Element Scanner results Delete-key handling through selection deletion: `cargo fmt --check`, `cargo test -p squalr --lib element_scanner_results_view`, and `cargo test -p squalr --lib`.
- Validation passed on 2026-05-08 after hardening Element Scanner result deletion: `cargo fmt --check`, `cargo test -p squalr-engine-api --lib snapshot`, `cargo test -p squalr --lib element_scanner_results_view_data`, `cargo test -p squalr-engine-scanning --lib`, `cargo test -p squalr-engine --lib scan_results`, `cargo test -p squalr --lib`, and `cargo test -p squalr-tests scan_results_delete`.
- Validation passed on 2026-05-08 after polishing takeover action sizing/padding and Symbol Struct Editor resolver pickers: `cargo fmt --check`, `cargo test -p squalr --lib symbol_struct_editor`, `cargo test -p squalr --lib symbol_resolver_editor`, `cargo test -p squalr --lib struct_viewer`, and `cargo test -p squalr --lib`.
- Validation passed on 2026-05-08 after removing Symbol Struct Editor expression authoring in favor of resolver-only dynamic counts/offsets: `cargo fmt --check`, `cargo test -p squalr --lib symbol_struct_editor`, `cargo test -p squalr-engine-domain --lib symbolic_resolver_definition`, `cargo test -p squalr-engine-domain --lib symbolic_struct_resolver`, and `cargo test -p squalr --lib`.
- Validation passed on 2026-05-08 after moving Symbol Struct Editor field details into Details Viewer: `cargo fmt --check`, `cargo test -p squalr --lib symbol_struct_editor`, `cargo test -p squalr --lib struct_viewer`, and `cargo test -p squalr --lib`.
- Validation passed on 2026-05-08 after polishing Symbol Struct Editor field entry rows: `cargo fmt --check`, `cargo test -p squalr --lib symbol_struct_editor`, and `cargo test -p squalr --lib`.
- Broader validation notes on 2026-05-08: full `cargo test -p squalr-tui` still fails unrelated `state::app_state::tests::memory_workspace_focus_cycle_loops_in_page_order`; full `cargo test -p squalr-engine --lib` still fails unrelated `execute_plugin_action_populates_pe_symbols`.
