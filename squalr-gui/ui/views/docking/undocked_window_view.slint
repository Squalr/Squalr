import { UndockedWindowTitleBarView } from "undocked_window_title_bar_view.slint";

export component UndockedWindowView inherits Window {
    preferred_width: 480px;
    preferred_height: 320px;
    no_frame: true;
    title: @tr("Tool");
    VerticalLayout {
        title_bar := UndockedWindowTitleBarView {
            title: @tr("Tool");
        }
    }
}
