<shell:Window
    x:Class="Squalr.View.Editors.ScriptEditor"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:avalonedit="http://icsharpcode.net/sharpdevelop/avalonedit"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:shell="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    x:Name="SqualrScriptEditor"
    Title="Script Editor"
    Width="512"
    Height="512"
    d:DesignHeight="256"
    d:DesignWidth="256"
    BorderBrush="{StaticResource SqualrColorWhite}"
    BorderThickness="2"
    DataContext="{Binding ScriptEditorViewModel, Source={StaticResource ViewModelLocator}}"
    WindowStartupLocation="CenterOwner"
    WindowStyle="None"
    mc:Ignorable="d">
    <shell:WindowChrome.WindowChrome>
        <shell:WindowChrome CaptionHeight="{Binding ActualHeight, ElementName=titlebar}" />
    </shell:WindowChrome.WindowChrome>

    <shell:Grid Background="{StaticResource SqualrColorFrame}">
        <shell:Grid.RowDefinitions>
            <shell:RowDefinition Height="Auto" />
            <shell:RowDefinition Height="Auto" />
            <shell:RowDefinition />
            <shell:RowDefinition Height="Auto" />
        </shell:Grid.RowDefinitions>

        <shell:Grid
            x:Name="titlebar"
            Grid.Row="0"
            Height="24"
            Background="{StaticResource SqualrColorFrame}">
            <shell:WrapPanel Margin="8,0,0,0" VerticalAlignment="Center">
                <shell:TextBlock
                    Foreground="{StaticResource SqualrColorWhite}"
                    Text="* "
                    Visibility="Collapsed" />
                <shell:TextBlock Foreground="{StaticResource SqualrColorWhite}" Text="Script Editor" />
            </shell:WrapPanel>
        </shell:Grid>


        <shell:Menu
            Grid.Row="1"
            Background="{StaticResource SqualrColorFrame}"
            BorderBrush="{StaticResource SqualrColorWhite}"
            Foreground="{StaticResource SqualrColorWhite}">
            <!--  File Header  -->
            <shell:MenuItem Header="File">
                <shell:MenuItem
                    Command="{Binding SaveCommand}"
                    Header="Save"
                    Style="{Binding}"
                    Visibility="Collapsed" />
                <shell:MenuItem
                    Click="ExitFileMenuItemClick"
                    Header="Exit"
                    Style="{Binding}" />
            </shell:MenuItem>
            <!--  Template Header  -->
            <shell:MenuItem Header="Templates">
                <shell:MenuItem
                    Click="CodeInjectionFileMenuItemClick"
                    Header="Code Injection"
                    Style="{Binding}" />
                <shell:MenuItem
                    Click="GraphicsOverlayFileMenuItemClick"
                    Header="Graphics Overlay"
                    Style="{Binding}" />
            </shell:MenuItem>
        </shell:Menu>

        <avalonedit:TextEditor
            Name="ScriptEditorTextEditor"
            Margin="8"
            shell:Grid.Row="2"
            Background="{StaticResource SqualrColorPanel}"
            Foreground="{StaticResource SqualrColorWhite}" />

        <shell:Grid
            Grid.Row="3"
            Margin="8"
            Background="{StaticResource SqualrColorFrame}">
            <shell:Button
                Width="96"
                Height="20"
                HorizontalAlignment="Left"
                VerticalAlignment="Bottom"
                Click="CancelButtonClick">
                <shell:TextBlock Text="Cancel" />
            </shell:Button>
            <shell:Button
                Width="96"
                Height="20"
                HorizontalAlignment="Right"
                VerticalAlignment="Bottom"
                shell:Grid.Column="1"
                Click="AcceptButtonClick">
                <shell:TextBlock Text="Accept" />
            </shell:Button>
        </shell:Grid>
    </shell:Grid>
</shell:Window>