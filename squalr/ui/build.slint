// Slint works by compiling things that inherit from Window, so these types are consoledated here
// such that Rust can compile them
import { MainWindowView } from "./views/main_window_view.slint";
import { PanelWindowView } from "./views/docking/panel_window_view.slint";

export { MainWindowView, PanelWindowView }
