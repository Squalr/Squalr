// Slint works by compiling things that inherit from Window, so these types are consoledated here
// such that Rust can compile them
import { MainWindowView } from "./views/main_window/main_window_view.slint";
import { UndockedWindowView } from "./views/docking/undocked_window_view.slint";

// Export the two primary window types -- the main window and panel windows.
export { MainWindowView, UndockedWindowView }

// Export all view model bindings such that Rust can interact with each view type through globals.
export * from "./view_model_bindings.slint";
