import { HorizontalBox, VerticalBox, Palette } from "std-widgets.slint";

import { Theme } from "../../theme.slint";

export component ProcessSelectorView inherits Rectangle {
    content := VerticalBox {
        padding: 0px;
        spacing: 0px;
        @children
    }
}
