import { Theme } from "../theme.slint";

import { DockRoot } from "./../components/docking/dock_root.slint";
import { HSeparator } from "./../components/separator.slint";
import { ToolbarMenu } from "./../components/toolbar_menu/toolbar_menu.slint";

import { FooterView } from "./footer_view.slint";
import { TitleBarView } from "./title_bar_view.slint";
import { ShortcutBarView } from "./shortcut_bar_view.slint";

export component MainWindowView inherits Window {
    title: @tr("Squalr");
    default-font-family: "Segoe UI";
    default-font-size: 10pt;
    default-font-weight: 525;
    preferred-width: 1280px;
    preferred-height: 840px;
    background: transparent;
    no-frame: true;
    VerticalLayout {
        title-bar := TitleBarView {
            title: @tr("Squalr");
        }

        toolbar-menu := ToolbarMenu {
            menus: [
                { header: "File", items:[{ text:"Select Project" }, { text:"Exit Squalr" }] },
                { header: "Edit", items:[{ text:"Settings" }] },
                { header: "Layout", items:[{ text:"Reset Layout" }] },
                {
                    header: "Windows",
                    items:[
                        { text:"Process Selector" },
                        { text:"Project Explorer" },
                        { text:"Property Viewer" },
                        { text:"Memory Viewer" },
                        { text:"Scan Results" },
                        { text:"Pointer Scan Results" },
                        { text:"Snapshot Manager" },
                        { text:".NET Explorer" },
                    ]
                },
                { header: "Scans", items:[{ text:"Pointer Scan" }] },
                { header: "Debugger", items:[{ text:"Disassembly" }, { text:"Code Tracer" }] },
            ];
        }

        shortcut-bar := ShortcutBarView { }

        dock-root := DockRoot { }

        footer := FooterView { }
    }
}
